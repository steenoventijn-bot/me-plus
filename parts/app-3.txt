orage.getItem('meplus-v2')||'null');if(l&&l.profile){let id=uid('user'),p={id,name:l.profile.name||'Jij',interests:l.profile.interests||[],events:l.events||[],goals:l.goals||[],completions:l.done||{},checkins:l.checkins||{},settings:{theme:l.settings?.theme||'auto'},social:{handle:''}};let x={version:2,currentProfileId:id,profiles:{[id]:p}};localStorage.setItem(KEY,JSON.stringify(x));return x}}catch{}return base()}
function save(){localStorage.setItem(KEY,JSON.stringify(state))}function prof(){return state.currentProfileId?state.profiles[state.currentProfileId]:null}function ensure(){let p=prof();if(!p)return;p.events||=[];p.goals||=[];p.completions||={};p.checkins||={};p.settings||={theme:'auto'};p.social||={handle:''}}
function pad(n){return String(n).padStart(2,'0')}function dateKey(d){return d.getFullYear()+'-'+pad(d.getMonth()+1)+'-'+pad(d.getDate())}function parse(k){let[y,m,d]=k.split('-').map(Number);return new Date(y,m-1,d,12)}function fmt(k){return new Intl.DateTimeFormat('nl-NL',{weekday:'long',day:'numeric',month:'long'}).format(parse(k))}function greet(){let h=new Date().getHours();return h>=5&&h<12?'Goedemorgen':h>=12&&h<18?'Goedemiddag':'Goedenavond'}function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function fact(){let d=parse(dateKey(new Date()));return FACTS[Math.abs(Math.floor(d.getTime()/86400000))%FACTS.length]}function occur(e,k){if(e.date===k)return true;if(!e.repeat||e.repeat==='none')return false;let a=parse(e.date),b=parse(k);if(b<a)return false;if(e.repeat==='daily')return true;if(e.repeat==='weekly')return a.getDay()===b.getDay();if(e.repeat==='monthly')return a.getDate()===b.getDate();return false}function events(k){return prof().events.filter(e=>occur(e,k)).sort((a,b)=>(a.start||'').localeCompare(b.start||''))}function doneKey(e,k){return(e.id||'')+'@'+k}function pct(){let p=prof(),now=new Date(),m=new Date(now);m.setDate(now.getDate()-((now.getDay()+6)%7));let t=0,d=0;for(let i=0;i<7;i++){let x=new Date(m);x.setDate(m.getDate()+i);let k=dateKey(x);events(k).filter(e=>e.type!=='appointment').forEach(e=>{t++;if(p.completions[doneKey(e,k)]==='done'||p.completions[doneKey(e,k)]===true)d++})}return t?Math.round(d/t*100):0}
function icon(n){let m={home:'<path d="M3 10.5 12 3l9 7.5v9a1.5 1.5 0 0 1-1.5 1.5h-5v-6h-5v6h-5A1.5 1.5 0 0 1 3 19.5z"/>',cal:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M8 3v4M16 3v4M3 10h18"/>',book:'<path d="M4 5.5A3.5 3.5 0 0 1 7.5 2H11v18H7.5A3.5 3.5 0 0 0 4 23zM20 5.5A3.5 3.5 0 0 0 16.5 2H13v18h3.5A3.5 3.5 0 0 1 20 23z"/>',msg:'<path d="M21 15a4 4 0 0 1-4 4H8l-5 3 1.5-5A7 7 0 0 1 3 12a8 8 0 0 1 8-8h2a8 8 0 0 1 8 8z"/>',user:'<circle cx="12" cy="8" r="4"/><path d="M4.5 21a7.5 7.5 0 0 1 15 0"/>'};return'<svg viewBox="0 0 24 24">'+m[n]+'</svg>'}
function nav(){return'<nav class="nav">'+[['today','home','Vandaag'],['agenda','cal','Agenda'],['learn','book','Leren'],['messages','msg','Berichten'],['profile','user','Profiel']].map(x=>`<button data-page="${x[0]}" class="${page===x[0]?'active':''}"><span class="ico">${icon(x[1])}</span>${x[2]}</button>`).join('')+'</nav>'}
function render(){ensure();applyTheme();let r=document.getElementById('root');if(!prof())return onboarding();r.innerHTML='<main class="app">'+(page==='today'?today():page==='agenda'?agenda():page==='learn'?learn():page==='messages'?messages():profilePage())+'</main>'+nav();bind()}
function today(){let p=prof(),k=dateKey(new Date()),f=fact(),es=events(k),growth=es.filter(e=>e.type!=='appointment'),d=growth.filter(e=>p.completions[doneKey(e,k)]==='done'||p.completions[doneKey(e,k)]===true).length;return`<header><p class="date">${fmt(k)}</p><h1 class="title">${greet()}, ${esc(p.name)}.</h1></header><section class="facthero" id="fact"><div class="factmeta"><span>Vandaag ontdekt</span><span>${f[0]}</span></div><h2>${f[1]}</h2><p>${f[2]}</p><div class="factextra"><p>${f[3]}</p></div><div class="factactions"><button class="textbtn" id="moreFact">Lees verder</button><button class="textbtn" data-page="learn">Meer ontdekken →</button></div></section><div class="sectionhead"><div><h2>Vandaag</h2><p>${d} van ${growth.length} verbeteractiviteiten afgerond</p></div><button class="link" id="add">+ Toevoegen</button></div><section class="card list">${es.length?es.map(e=>item(e,k)).join(''):'<div class="empty">Je dag is nog rustig. Plan alleen wat vandaag echt aandacht verdient.</div>'}</section><div class="overview"><button class="ov" data-page="profile"><span>Deze week</span><strong>${pct()}%</strong><small>van je verbeteractiviteiten</small></button><button class="ov" data-page="profile"><span>Doelen</span><strong>${p.goals.length}</strong><small>jouw huidige focus</small></button></div>`}
function item(e,k){let done=prof().completions[doneKey(e,k)]==='done'||prof().completions[doneKey(e,k)]===true;return`<article class="item"><div class="itime">${e.start||''}<br>${e.end||''}</div><div><div class="ititle">${esc(e.title)}</div><div class="imeta">${e.type==='appointment'?'Afspraak':'Verbeteractiviteit'}${e.category?' · '+esc(e.category):''}</div></div>${e.type==='appointment'?'<span>·</span>':`<button class="check ${done?'done':''}" data-done="${e.id}" data-date="${k}">${done?'✓':'○'}</button>`}</article>`}
function agenda(){let y=month.getFullYear(),m=month.getMonth(),first=new Date(y,m,1),offset=(first.getDay()+6)%7,days=new Date(y,m+1,0).getDate(),cells='';for(let i=0;i<offset;i++)cells+='<div></div>';for(let d=1;d<=days;d++){let k=dateKey(new Date(y,m,d)),has=events(k).length;cells+=`<button class="day ${k===dateKey(new Date())?'today':''} ${k===sel?'selected':''} ${has?'has':''}" data-date="${k}">${d}</button>`}return`<div class="pagehead"><p class="eyebrow">Planning</p><h1>Agenda</h1><p>Afspraken en verbeteractiviteiten op één rustige plek.</p></div><section class="card"><div class="sectionhead" style="margin-top:0"><button class="link" id="prev">‹</button><h2>${new Intl.DateTimeFormat('nl-NL',{month:'long',year:'numeric'}).format(month)}</h2><button class="link" id="next">›</button></div><div class="calendar">${['ma','di','wo','do','vr','za','zo'].map(x=>`<div class="dow">${x}</div>`).join('')}${cells}</div></section><div class="sectionhead"><div><h2>${fmt(sel)}</h2><p>${events(sel).length} gepland</p></div><button class="link" id="add">+ Toevoegen</button></div><section class="card list">${events(sel).length?events(sel).map(e=>item(e,sel)).join(''):'<div class="empty">Niets gepland op deze dag.</div>'}</section><button class="fab" id="fab">+</button>`}
function learn(){let f=fact();return`<div class="pagehead"><p class="eyebrow">Leren</p><h1>Elke dag iets nieuws.</h1><p>Korte ontdekkingen voor tussendoor, of één onderwerp waarin je echt verder duikt.</p></div><section class="learnfeature"><div class="eyebrow">Uitgelicht · ${f[0]}</div><h2>${f[1]}</h2><p>${f[2]} ${f[3]}</p></section><div class="sectionhead"><div><h2>Hoeveel tijd heb je?</h2><p>${LESSON.title}</p></div></div><div class="durations">${[5,10,20].map(n=>`<button class="duration" data-min="${n}"><strong>${n}</strong><span>minuten</span><small>${n===5?'De kern':n===10?'Met verdieping':'Volledige mini-les'}</small></button>`).join('')}</div><div class="sectionhead"><div><h2>Ontdek meer</h2><p>Tik op een weetje om verder te lezen.</p></div></div><div class="feed">${FACTS.map((f,i)=>`<article data-feed="${i}"><div class="eyebrow">${f[0]}</div><h3>${f[1]}</h3><p>${f[2]}</p><p class="feedextra">${f[3]}</p></article>`).join('')}</div>`}
function messages(){let p=prof();return`<div class="pagehead"><p class="eyebrow">Berichten</p><h1>Mensen die je kent.</h1><p>Een rustige plek voor vrienden en privéberichten, zonder openbare sociale feed.</p></div><section class="card"><div class="eyebrow">Jouw gebruikersnaam</div><div class="field"><span>Hoe vrienden je straks kunnen vinden</span><input class="input" id="handle" placeholder="bijv. tijn" value="${esc(p.social.handle||'')}"></div><button class="btn primary" id="saveHandle">Bewaren</button></section><section class="socialbox"><div class="socialicon">${icon('msg')}</div><h2>Vrienden & chat worden cloud-gekoppeld</h2><p>Zoeken op gebruikersnaam, volgverzoeken, accepteren/weigeren en echte privéberichten worden pas aangezet zodra profielen veilig tussen verschillende telefoons kunnen synchroniseren. Hier staat bewust geen nep-chat achter.</p></section>`}
function profilePage(){let p=prof();return`<div class="pagehead"><p class="eyebrow">Jouw omgeving</p><h1>Profiel</h1><p>Je naam, doelen en voorkeuren.</p></div><section class="card"><div class="field"><span>Naam</span><input class="input" id="name" value="${esc(p.name)}"></div><button class="btn primary" id="saveName">Opslaan</button></section><div class="sectionhead"><div><h2>Doelen</h2><p>${p.goals.length} actief</p></div><button class="link" id="addGoal">+ Doel</button></div><section class="card list">${p.goals.length?p.goals.map(g=>`<div class="item" style="grid-template-columns:1fr auto