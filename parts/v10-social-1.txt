/* me+ v10 — social/game product layer */
const V10_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-social-api';
const V10_ACCENT_KEY='meplus-accent-v10';
const V10_EMOJIS=['❤️','🔥','😂','👏','💪','⚡'];
const V10_METRICS=[
 ['points','Punten','Meeste Plus-punten','🏆'],
 ['activities','Activiteiten','Meeste verbeteractiviteiten','🔥'],
 ['knowledge','Kennis','Meeste goede quizantwoorden','📚'],
 ['active_days','Actieve dagen','Meeste dagen met een punt','⚡'],
 ['custom','Eigen doel','Houd zelf je score bij','🎯']
];
const V10_DURATIONS=[[24,'24 uur','Snel & intens'],[72,'3 dagen','Korte sprint'],[168,'7 dagen','De ultieme challenge'],[336,'14 dagen','Voor de volhouders']];
server.v10=server.v10||{me:null,battles:{incoming:[],outgoing:[],active:[],recent:[]},leaderboard:[],chatFriend:null,chatMessages:[],friendProfile:null};
let v10BattleTimer=null,v10ChatTimer=null,v10HoldTimer=null,v10ReplyTo=null;

function api10(op,data={}){return call(V10_API,op,data)}
function v10ApplyAccent(theme){theme=theme||localStorage.getItem(V10_ACCENT_KEY)||server.v10?.me?.accentTheme||'fresh';localStorage.setItem(V10_ACCENT_KEY,theme);document.documentElement.dataset.accent=theme}
function v10Avatar(p,size='md'){const c=p?.avatarConfig||{},bg=c.background||'mint',tone=c.tone||'warm',hair=c.hair||'wave',outfit=c.outfit||'tee',acc=c.accessory||'none';const hairPath=hair==='buzz'?'<path d="M20 25c3-10 21-10 24 0-5-5-19-5-24 0Z"/>':hair==='long'?'<path d="M18 24c3-14 25-14 28 0v19H18Z"/>':hair==='curl'?'<path d="M18 26c-1-12 29-16 28 2-4-6-8-8-14-8s-10 2-14 6Z"/>':hair==='short'?'<path d="M19 25c4-11 23-12 26-1-9-4-18-5-26 1Z"/>':'<path d="M18 26c4-14 25-14 28-1-5-4-8-5-12-4-5 1-8-1-16 5Z"/>';const accSvg=acc==='glasses'?'<path class="line" d="M22 30h8m4 0h8M30 30h4"/><circle class="line" cx="26" cy="30" r="4"/><circle class="line" cx="38" cy="30" r="4"/>':acc==='cap'?'<path d="M19 22c7-7 20-7 27 0v4H19Z"/><path d="M43 25h9v3h-9Z"/>':acc==='headband'?'<path class="line wide" d="M20 24c8-5 17-5 25 0"/>':acc==='crown'?'<path class="gold" d="m22 21 5 4 5-8 5 8 5-4-2 9H24Z"/>':'';return `<div class="v10-avatar ${size} bg-${bg}" aria-hidden="true"><svg viewBox="0 0 64 64"><circle class="skin tone-${tone}" cx="32" cy="31" r="15"/><g class="hair">${hairPath}</g><path class="shirt outfit-${outfit}" d="M10 62c2-15 11-22 22-22s20 7 22 22Z"/><circle cx="27" cy="31" r="1.5"/><circle cx="37" cy="31" r="1.5"/><path class="line" d="M28 37c3 2 5 2 8 0"/>${accSvg}</svg></div>`}
function v10Metric(metric){return V10_METRICS.find(x=>x[0]===metric)||V10_METRICS[0]}
function v10DurationLabel(hours){return hours<48?`${hours} uur`:`${Math.round(hours/24)} dagen`}
function v10TimeLeft(end){if(!end)return'';let ms=Math.max(0,new Date(end).getTime()-Date.now()),d=Math.floor(ms/86400000);ms%=86400000;const h=Math.floor(ms/3600000),m=Math.floor((ms%3600000)/60000);return d?`${d}d ${h}u`:h?`${h}u ${m}m`:`${m}m`}
function v10SelfBadges(){const s=server.stats||{},b=server.v10?.battles?.recent||[],wins=b.filter(x=>x.winner_id===server.profile?.id).length,st=server.extras?.streak||0,pts=server.profile?.points||0;return [st>=7?'🔥 Streak Master':null,wins>=3?'⚔️ Battle Beast':null,(s.reads||0)>=10?'📚 Knowledge':null,pts>=100?'🏆 Century':null,(s.activities||0)>=25?'💪 Consistent':null].filter(Boolean)}
function v10Collectibles(){const p=server.profile?.points||0,xp=server.v10?.me?.xp||0;return [{icon:'🦆',name:'Classic Duck',on:true},{icon:'✨',name:'Glow',on:p>=25},{icon:'👑',name:'Crown',on:p>=100},{icon:'⚔️',name:'Battle Aura',on:xp>=100}]}

const v10RefreshBase=refreshBase;
refreshBase=async function(){await v10RefreshBase();try{const g=await apiProfile11('me');if(g.profile)v13q4ApplyProfile(g.profile)}catch(e){console.warn('v10 profile',e)}}

async function loadSocial(tab='hub'){try{const jobs=[api('friends'),api10('battle_list'),api10('friend_leaderboard',{period:socialPeriod}),api('conversations')];const [fr,ba,lb,co]=await Promise.allSettled(jobs);if(fr.status==='fulfilled'){server.friends=fr.value.friends||[];server.incomingRequests=fr.value.incomingRequests||[];server.outgoingRequests=fr.value.outgoingRequests||[]}if(ba.status==='fulfilled')server.v10.battles=ba.value;if(lb.status==='fulfilled')server.v10.leaderboard=lb.value.rows||[];if(co.status==='fulfilled')server.conversations=co.value.conversations||[];socialTab='hub'}catch(e){toast(e.message)}}

function v10BattleMini(b){const other=b.profiles?.challenger?.id===server.profile?.id?b.profiles?.opponent:b.profiles?.challenger,metric=v10Metric(b.metric);return `<button class="v10-battle-mini" data-v10-battle="${b.id}"><div class="v10-battle-top"><span>${metric[3]} ${esc(metric[1])}</span><b>${v10TimeLeft(b.ends_at)}</b></div><div class="v10-versus">${v10Avatar(server.v10.me||server.profile,'sm')}<strong>VS</strong>${v10Avatar(other,'sm')}</div><div class="v10-battle-names"><span>Jij</span><span>${esc(other?.name||'Vriend')}</span></div></button>`}
function v10InviteMini(b){const who=b.profiles?.challenger,metric=v10Metric(b.metric);return `<div class="v10-invite-card"><div class="v10-invite-person">${v10Avatar(who,'sm')}<div><b>${esc(who?.name||'Vriend')} daagt je uit</b><span>${metric[3]} ${esc(metric[2])} · ${v10DurationLabel(b.duration_hours)}</span></div></div><div class="v10-invite-actions"><button class="button line small" data-v10-decline="${b.id}">Weiger</button><button class="button primary small" data-v10-accept="${b.id}">Accepteer</button></div></div>`}
function v10FriendCard(p){return `<button class="v10-friend-card" data-v10-profile="${p.id}">${v10Avatar(p,'md')}<div><b>${esc(p.name)}</b><span>@${esc(p.handle)}</span></div><i>›</i></button>`}
function v10LeaderboardRows(){const rows=server.v10.leaderboard||[];if(!rows.length)return'<div class="empty">De ranglijst komt tot leven zodra vrienden voortgang delen.</div>';return rows.slice(0,5).map((p,i)=>`<button class="v10-leader" ${p.isMe?'':'data-v10-profile="'+p.id+'"'}><span class="v10-place p${i+1}">${i+1}</span>${v10Avatar(p,'xs')}<div><b>${esc(p.name)}${p.isMe?' · jij':''}</b><span>${esc(p.rank||'')} · level ${p.level||1}</span></div><strong>${p.score}</strong></button>`).join('')}
function v10Conversation(p){return `<button class="v10-conversation" data-v10-chat="${p.id}" data-v10-name="${esc(p.name)}">${v10Avatar(p,'sm')}<div><b>${esc(p.name)}</b><span>${esc(p.last?.body||'Start een gesprek')}</span></div><i>›</i></button>`}

function socialView(){const b=server.v10.battles||{incoming:[],active:[]},req=server.incomingRequests.length||0;return shell(`<section class="v10-social-hero"><div><span class="v10-kicker">Become better. Together.</span><h1>Vrienden</h1><p>Samen groeien voelt beter als er iets op het spel staat.</p></div><button id="v10-add-friend" class="v10-round-plus">+</button></section>${req?`<button class="v10-request-banner" id="v10-show-requests"><b>${req} nieuw${req===1?'':'e'} vriendverzoek${req===1?'':'en'}</b><span>Bekijk en reageer →</span></button>`:''}${b.incoming?.length?`<section class="v10-zone rose"><div class="v10-zone-head"><div><span>Uitdagingen</span><h2>Jij bent uitgedaagd</h2></div><div class="v10-zone-icon">⚔️</div></div>${b.incoming.map(v10InviteMini).join('')}</section>`:''}<section class="v10-section"><div class="v10-section-head"><div><span>🔥 Actieve battles</span><h2>Strijd om de eerste plek</h2></div><button id="v10-new-battle">Nieuwe battle</button></div>${b.active?.length?`<div class="v10-battle-strip">${b.active.map(v10BattleMini).join('')}</div>`:`<button class="v10-empty-battle" id="v10-empty-battle"><div>⚔️</div><b>Daag een vriend uit</b><span>24 uur, 7 dagen of jouw eigen challenge.</span></button>`}</section><section class="v10-zone gold"><div class="v10-zone-head"><div><span>👑 Ranglijst</span><h2>Deze week</h2></div><button class="v10-text-btn" id="v10-period">${socialPeriod==='week'?'Week':socialPeriod==='month'?'Maand':'All time'} ↻</button></div><div class="v10-leaderboard">${v10LeaderboardRows()}</div></section><section class="v10-section"><div class="v10-section-head"><div><span>👥 Jouw crew</span><h2>Vrienden</h2></div><button id="v10-add-friend-2">Voeg toe</button></div><div class="v10-friend-grid">${server.friends.length?server.friends.map(v10FriendCard).join(''):'<div class="empty">Nog geen vrienden. Voeg iemand toe om samen te starten.</div>'}</div></section><section class="v10-zone blue"><div class="v10-zone-head"><div><span>💬 Berichten</span><h2>Je gesprekken</h2></div></div>${server.conversations.length?server.conversations.slice(0,5).map(v10Conversation).join(''):'<div class="empty">Nog geen gesprekken.</div>'}</section>`) }

function v10OpenFriendSearch(){const w=openSheet(`<div class="sheet-top"><div><h2>Vriend toevoegen</h2><p class="lead">Zoek op naam of @gebruikersnaam.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="search"><input id="v10-friend-q" placeholder="Naam of @gebruikersnaam"><button id="v10-friend-go" class="button primary small">Zoek</button></div><div id="v10-search-results"></div>${server.incomingRequests.length?`<section class="section"><div class="section-head"><h2>Verzoeken</h2></div>${server.incomingRequests.map(requestRow).join('')}</section>`:''}`);w.querySelector('[data-close]').onclick=()=>w.remove();const go=async()=>{const q=w.querySelector('#v10-friend-q').value.trim(),target=w.querySelector('#v10-search-results');if(q.length<2)return toast('Typ minimaal twee letters.');target.innerHTML='<div class="empty">Zoeken…</div>';try{const r=await api('search',{q});target.innerHTML=(r.users||[]).map(searchRow).join('')||'<div class="empty">Niemand gevonden.</div>';target.querySelectorAll('[data-request]').forEach(b=>b.onclick=async()=>{await friendAction('friend_request',b.dataset.request);w.remove();await loadSocial();render()});target.querySelectorAll('[data-accept]').forEach(b=>b.onclick=async()=>{await friendAction('respond_request',b.dataset.accept,{action:'accept'});w.remove();await loadSocial();render()});target.querySelectorAll('[data-cancel-request]').forEach(b=>b.onclick=async()=>{await friendAction('cancel_request',b.dataset.cancelRequest);w.remove();await loadSocial();render()})}catch(e){target.innerHTML=`<div class="empty">${esc(e.message)}</div>`}};w.querySelector('#v10-friend-go').onclick=go;w.querySelector('#v10-friend-q').onkeydown=e=>{if(e.key==='Enter')go()};w.querySelectorAll('[data-accept]').forEach(b=>b.onclick=async()=>{await friendAction('respond_request',b.dataset.accept,{action:'accept'});w.remove();await loadSocial();render()});w.querySelectorAll('[data-reject]').forEach(b=>b.onclick=async()=>{await friendAction('respond_request',b.dataset.reject,{action:'reject'});w.remove();await loadSocial();render()})}

function v142BattleRule(metric){return ({
 points:'Verdien Plus-punten met verbeteractiviteiten en goed beantwoorde kennisquizzen. Je nieuwe punten tellen automatisch mee.',
 activities:'Rond verbeteractiviteiten af en voeg het gevraagde bewijs toe. Elke activiteit waarvoor je een punt krijgt, telt automatisch als 1.',
 knowledge:'Lees een tekst bij Kennis en beantwoord de quizvraag goed. Elke quiz waarvoor je een punt krijgt, telt automatisch als 1. Een fout antwoord telt niet mee.',
 active_days:'Verdien op een dag minstens één Plus-punt. Die dag telt automatisch als 1 actieve dag, ook als je meer punten verdient. Alleen de app openen telt niet.',
 custom:'Spreek samen af wat één keer telt, bijvoorbeeld één hardloopsessie. Tik na elke keer op “+ 1 keer gedaan” in deze battle. Jullie houden dit zelf eerlijk bij. Het streefdoel stopt de battle niet: de hoogste score aan het einde wint.'
 })[metric]||'Alleen voortgang tijdens deze battle telt mee.'}
function v142BattleHelp(){return `<section class="v142-battle-help"><h2>Zo werken battles</h2><p>Daag een vriend uit en probeer binnen de afgesproken tijd de hoogste score te halen.</p><ol><li><b>Kies samen de uitdaging.</b> Kies wat telt en hoe lang jullie spelen.</li><li><b>Je vriend accepteert → de tijd start.</b> Jullie beginnen op 0. Eerdere punten tellen niet mee.</li><li><b>Verzamel score vóór de tijd om is.</b> Meestal gaat dit automatisch; bij een eigen doel voer je zelf elke keer in.</li></ol><p><b>Hoogste score aan het einde wint: +50 XP.</b> Evenveel? Dan is het gelijkspel en krijgt niemand de winbonus.</p><details><summary>Voorbeeld: een puntenbattle</summary><p>Jullie kiezen 3 dagen. Na accepteren verdien jij 8 nieuwe Plus-punten en je vriend 6. Als de tijd om is, win jij. Je totale punten van vóór de battle maken niet uit.</p></details></section>`}
function v142BattleGuide(b){return `<section class="v142-battle-help"><h2>Wat telt in deze battle?</h2><p>${esc(v142BattleRule(b.metric))}</p>${b.metric==='custom'&&b.custom_target?`<p>Streefdoel: <b>${Number(b.custom_target)} keer</b>.</p>`:''}<p>${b.status==='pending'?'<b>De tijd en score starten pas na accepteren.</b> Jullie beginnen op 0.':'Alleen verdiende score tussen de start en het einde telt mee.'} De hoogste score aan het einde wint <b>50 XP</b>. Bij gelijkspel krijgt niemand de winbonus.</p></section>`}
