/* me+ v11 — central account + persistent user state */
const V11_ACCOUNT_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-account-api';
const V11_STATE_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-state-api';
const V11_PROFILE_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-v11-profile-api';
let v11StateReady=false,v11StateTimer=null,v11SocialTimer=null,v11AccountProfile=null;
function apiAccount(op,data={}){return call(V11_ACCOUNT_API,op,data)}
function apiState(op,data={}){return call(V11_STATE_API,op,data)}
function apiProfile11(op,data={}){return call(V11_PROFILE_API,op,data)}
function v11EmptyState(name=''){return{profileName:name,events:[],done:{},goals:[],checkins:{},knowledgeSeen:false,focusId:null}}
function v11StoreSession(r){if(r?.token)localStorage.setItem(DEVICE_KEY,r.token);if(r?.profile){server.profile=r.profile;server.v10.me={...(server.v10.me||{}),...r.profile};v11AccountProfile=r.profile;state.profileName=r.profile.name||state.profileName;save()}}
async function v11LoadServerState(seedLocal=true){try{const r=await apiState('load');if(r.state){state={...v11EmptyState(server.profile?.name||state.profileName),...r.state,profileName:server.profile?.name||r.state.profileName||state.profileName};localStorage.setItem(STATE_KEY,JSON.stringify(state));v11StateReady=true;return}if(seedLocal){const s=await apiState('seed',{state});if(s.state)state={...v11EmptyState(server.profile?.name||state.profileName),...s.state,profileName:server.profile?.name||state.profileName};localStorage.setItem(STATE_KEY,JSON.stringify(state))}v11StateReady=true}catch(e){console.warn('state load',e);v11StateReady=true}}
const v11LocalSave=save;
save=function(){v11LocalSave();if(!v11StateReady||!server.profile?.id)return;clearTimeout(v11StateTimer);v11StateTimer=setTimeout(()=>apiState('save',{state}).catch(e=>console.warn('state save',e)),450)}
async function v11AccountBoot(){const started=Date.now();v9ApplyTheme?.();launchV8?.();registerSW();try{const s=await apiAccount('session');v11StoreSession(s);await v11LoadServerState(true);await refreshBase();try{const p=await apiProfile11('me');if(p.profile){server.v10.me={...(server.v10.me||{}),...p.profile};server.profile={...(server.profile||{}),...p.profile}}}catch{}await onAppOpen();await syncAllReminders();if(view==='social')await loadSocial();render();setTimeout(()=>{finishLaunchV8?.();maybeStreakMilestoneV8?.()},Math.max(0,1050-(Date.now()-started)))}catch(e){finishLaunchV8?.();renderOnboarding()}}
boot=v11AccountBoot;

function renderOnboarding(){const app=el('app');if(!app)return;app.innerHTML=`<section class="v11-auth"><div class="v11-auth-card"><div class="v11-auth-brand">me<span>+</span></div><span class="v11-auth-kicker">Jouw groei. Eén account.</span><h1>Welkom bij Me+</h1><p>Je profiel, vrienden, voortgang en battles blijven voortaan aan hetzelfde account gekoppeld.</p><div class="v11-auth-tabs"><button data-auth-tab="login" class="active">Inloggen</button><button data-auth-tab="create">Account maken</button></div><div id="v11-login" class="v11-auth-panel active"><label>Gebruikersnaam of naam<input id="v11-login-name" autocomplete="username" placeholder="bijv. kees-de-kees"></label><label>Wachtwoord<input id="v11-login-pass" type="password" autocomplete="current-password" placeholder="Minimaal 8 tekens"></label><button id="v11-login-go" class="button primary full">Inloggen</button></div><div id="v11-create" class="v11-auth-panel"><label>Je naam<input id="v11-create-name" maxlength="40" autocomplete="name" placeholder="Voor- en achternaam"></label><label>Wachtwoord<input id="v11-create-pass" type="password" autocomplete="new-password" placeholder="Minimaal 8 tekens"></label><button id="v11-create-go" class="button primary full">Account maken</button></div><div class="v11-auth-note">Geen e-mail nodig. Je krijgt een unieke @gebruikersnaam op basis van je naam.</div></div></section>`;
 document.querySelectorAll('[data-auth-tab]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-auth-tab]').forEach(x=>x.classList.toggle('active',x===b));el('v11-login').classList.toggle('active',b.dataset.authTab==='login');el('v11-create').classList.toggle('active',b.dataset.authTab==='create')});
 el('v11-login-go').onclick=async()=>{const btn=el('v11-login-go'),login=el('v11-login-name').value.trim(),password=el('v11-login-pass').value;btn.disabled=true;btn.textContent='Inloggen…';try{const r=await apiAccount('login',{login,password});state=v11EmptyState(r.profile?.name||'');v11StateReady=false;localStorage.setItem(STATE_KEY,JSON.stringify(state));v11StoreSession(r);await v11LoadServerState(false);await v11AccountBoot()}catch(e){toast(e.message);btn.disabled=false;btn.textContent='Inloggen'}};
 el('v11-create-go').onclick=async()=>{const btn=el('v11-create-go'),name=el('v11-create-name').value.trim(),password=el('v11-create-pass').value;btn.disabled=true;btn.textContent='Account maken…';try{const r=await apiAccount('register',{name,password});state=v11EmptyState(r.profile?.name||name);v11StateReady=false;localStorage.setItem(STATE_KEY,JSON.stringify(state));v11StoreSession(r);await apiState('seed',{state});v11StateReady=true;await v11AccountBoot()}catch(e){toast(e.message);btn.disabled=false;btn.textContent='Account maken'}}}

function v11AccountSheet(){const p=v11AccountProfile||server.profile||{},ready=!!p.accountReady;const w=openSheet(`<div class="sheet-top"><div><h2>Account</h2><p class="lead">Eén profiel op al je apparaten.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div>${ready?`<div class="v11-account-ready"><b>✓ Account beveiligd</b><span>Log op een ander apparaat in met <strong>@${esc(p.handle||'')}</strong>.</span></div><button id="v11-change-pass" class="button line full">Wachtwoord wijzigen</button><button id="v11-logout" class="button danger full">Uitloggen op dit apparaat</button>`:`<div class="notice"><b>Nog één stap:</b><br>Stel een wachtwoord in. Daarna kun je met precies ditzelfde profiel op telefoon én laptop inloggen.</div><label class="field"><span>Nieuw wachtwoord</span><input id="v11-claim-pass" type="password" autocomplete="new-password" placeholder="Minimaal 8 tekens"></label><label class="field"><span>Herhaal wachtwoord</span><input id="v11-claim-pass2" type="password" autocomplete="new-password"></label><button id="v11-claim" class="button primary full">Account beveiligen</button>`}`);w.querySelector('[data-close]').onclick=()=>w.remove();
 const claim=async()=>{const a=w.querySelector('#v11-claim-pass')?.value||w.querySelector('#v11-new-pass')?.value||'',b=w.querySelector('#v11-claim-pass2')?.value||w.querySelector('#v11-new-pass2')?.value||'';if(a.length<8)return toast('Gebruik minimaal 8 tekens.');if(a!==b)return toast('De wachtwoorden zijn niet hetzelfde.');try{const r=await apiAccount('claim',{password:a});v11StoreSession(r);v11AccountProfile=r.profile;await v11LoadServerState(true);w.remove();render();toast('Account beveiligd ✓')}catch(e){toast(e.message)}};
 w.querySelector('#v11-claim')?.addEventListener('click',claim);w.querySelector('#v11-change-pass')?.addEventListener('click',()=>{w.innerHTML=`<div class="sheet"><div class="sheet-bar"></div><div class="sheet-top"><div><h2>Wachtwoord wijzigen</h2><p class="lead">Je profiel blijft hetzelfde.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><label class="field"><span>Nieuw wachtwoord</span><input id="v11-new-pass" type="password"></label><label class="field"><span>Herhaal wachtwoord</span><input id="v11-new-pass2" type="password"></label><button id="v11-claim" class="button primary full">Opslaan</button></div>`;w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelector('#v11-claim').onclick=claim});w.querySelector('#v11-logout')?.addEventListener('click',()=>{localStorage.removeItem(DEVICE_KEY);state=v11EmptyState();localStorage.setItem(STATE_KEY,JSON.stringify(state));server.profile=null;server.v10.me=null;v11StateReady=false;w.remove();renderOnboarding()})}

const v11ProfileBase=profileView;
profileView=function(){const html=v11ProfileBase();return html.replace('</main>',`<section class="section v11-account-entry"><div class="menu"><button class="menu-btn" id="v11-account"><span class="menu-icon">${svg('shield')}</span><span class="menu-main"><span class="menu-title">Account & apparaten</span><span class="menu-sub">${server.profile?.accountReady?'Beveiligd · @'+esc(server.profile.handle||''):'Stel een wachtwoord in om op meerdere apparaten in te loggen'}</span></span><span class="chev">›</span></button></div></section></main>`)};

const v11BindBase=bind;
bind=function(){v11BindBase();el('v11-account')?.addEventListener('click',v11AccountSheet)};

async function v11RefreshSocialNow(){if(view!=='social'||document.hidden)return;try{const oldReq=server.incomingRequests?.length||0;await loadSocial();try{const s=await apiProfile11('social_summaries');const byId=new Map((s.friends||[]).map(x=>[x.id,x]));server.friends=(server.friends||[]).map(x=>({...x,...(byId.get(x.id)||{})}));const inMap=new Map((s.incoming||[]).map(x=>[x.id,x]));server.incomingRequests=(server.incomingRequests||[]).map(x=>({...x,...(inMap.get(x.id)||{})}));const outMap=new Map((s.outgoing||[]).map(x=>[x.id,x]));server.outgoingRequests=(server.outgoingRequests||[]).map(x=>({...x,...(outMap.get(x.id)||{})}))}catch{}if(view==='social'){render();if((server.incomingRequests?.length||0)>oldReq)hapticV8?.()}}catch(e){console.warn('social refresh',e)}}
clearInterval(v11SocialTimer);v11SocialTimer=setInterval(v11RefreshSocialNow,5000);
