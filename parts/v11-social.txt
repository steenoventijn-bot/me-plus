/* me+ v11 — friend tabs, enriched profiles, custom battle duration */
let v11FriendsTab='friends';
function v11Status(p){return p.online?'● Nu actief':p.lastSeenAt?'Recent actief':'Offline'}
function v11FriendFull(p){return `<article class="v11-friend-full" data-v11-profile-card="${p.id}"><div class="v11-friend-main">${v10Avatar(p,'md')}<div><b>${esc(p.name)}</b><span>@${esc(p.handle||'')} · ${v11Status(p)}</span></div><i>›</i></div><div class="v11-friend-stats"><span><b>Lv. ${p.level||1}</b><small>${esc(p.rank||'')}</small></span><span><b>${p.xp||0}</b><small>XP</small></span><span><b>🔥 ${p.streak||0}</b><small>streak</small></span><span><b>⭐ ${p.points||0}</b><small>punten</small></span><span><b>🏆 ${p.wins||0}</b><small>wins</small></span></div><div class="v11-friend-actions"><button data-v11-message="${p.id}" data-v11-name="${esc(p.name)}">Bericht</button><button data-v11-challenge="${p.id}">Daag uit</button></div></article>`}
function v11RequestCard(p,incoming=true){return `<article class="v11-request-card">${v10Avatar(p,'md')}<div class="v11-request-person"><b>${esc(p.name)}</b><span>@${esc(p.handle||'')} · Level ${p.level||1}</span><div><i>⭐ ${p.points||0}</i><i>🔥 ${p.streak||0}</i></div></div>${incoming?`<div class="v11-request-actions"><button data-reject="${p.id}">Weigeren</button><button data-accept="${p.id}">Accepteren</button></div>`:`<div class="v11-request-sent">Verzoek verzonden ✓<button data-cancel-request="${p.id}">Intrekken</button></div>`}</article>`}
function v11BattleHub(){const b=server.v10.battles||{incoming:[],active:[],recent:[]};return `<section class="v10-zone rose"><div class="v10-zone-head"><div><span>Uitdagingen</span><h2>Openstaande battles</h2></div><button id="v10-new-battle" class="v10-text-btn">Nieuwe battle +</button></div>${b.incoming?.length?b.incoming.map(v10InviteMini).join(''):'<div class="empty">Geen nieuwe uitdagingen.</div>'}</section><section class="v10-section"><div class="v10-section-head"><div><span>🔥 Actief</span><h2>Live battles</h2></div></div>${b.active?.length?`<div class="v10-battle-strip">${b.active.map(v10BattleMini).join('')}</div>`:`<button class="v10-empty-battle" id="v10-empty-battle"><div>⚔️</div><b>Start een battle</b><span>Kies doel, duur en tegenstander.</span></button>`}</section><section class="v10-zone gold"><div class="v10-zone-head"><div><span>👑 Leaderboard</span><h2>${socialPeriod==='week'?'Deze week':socialPeriod==='month'?'Deze maand':'All time'}</h2></div><button id="v10-period" class="v10-text-btn">Wissel ↻</button></div><div class="v10-leaderboard">${v10LeaderboardRows()}</div></section>${b.recent?.length?`<section class="v10-section"><div class="v10-section-head"><div><span>Geschiedenis</span><h2>Recente battles</h2></div></div><div class="v11-battle-history">${b.recent.slice(0,8).map(x=>{const other=x.profiles?.challenger?.id===server.profile?.id?x.profiles?.opponent:x.profiles?.challenger;return `<button data-v10-battle="${x.id}"><span>${x.winner_id===server.profile?.id?'🏆':'⚔️'}</span><div><b>${esc(other?.name||'Vriend')}</b><small>${esc(v10Metric(x.metric)[2])}</small></div><i>›</i></button>`}).join('')}</div></section>`:''}`}
function v11FriendsContent(){if(v11FriendsTab==='requests')return `<section class="v11-tab-intro"><h2>Vriendverzoeken</h2><p>Nieuwe connecties worden direct aan het juiste account gekoppeld.</p></section>${server.incomingRequests.length?`<section class="v11-requests"><span class="v11-label">ONTVANGEN</span>${server.incomingRequests.map(p=>v11RequestCard(p,true)).join('')}</section>`:'<div class="empty">Geen openstaande verzoeken.</div>'}${server.outgoingRequests.length?`<section class="v11-requests"><span class="v11-label">VERSTUURD</span>${server.outgoingRequests.map(p=>v11RequestCard(p,false)).join('')}</section>`:''}`;if(v11FriendsTab==='battles')return v11BattleHub();return `<div class="v11-friends-tools"><button id="v10-add-friend">＋ Vriend toevoegen</button><button id="v10-new-battle">⚔️ Nieuwe battle</button></div><section class="v11-friends-list">${server.friends.length?server.friends.map(v11FriendFull).join(''):'<div class="empty">Nog geen vrienden. Zoek iemand op naam of @gebruikersnaam.</div>'}</section>${server.conversations.length?`<section class="v10-zone blue"><div class="v10-zone-head"><div><span>💬 Berichten</span><h2>Recente gesprekken</h2></div></div>${server.conversations.slice(0,5).map(v10Conversation).join('')}</section>`:''}`}
function socialView(){const req=server.incomingRequests.length||0,battles=(server.v10.battles?.incoming||[]).length;return shell(`<section class="v10-social-hero v11-social-hero"><div><span class="v10-kicker">Become better. Together.</span><h1>Vrienden</h1><p>Vind elkaar, praat, daag uit en groei samen.</p></div><button id="v10-add-friend-2" class="v10-round-plus">+</button></section><div class="v11-social-tabs"><button data-v11-friends-tab="friends" class="${v11FriendsTab==='friends'?'active':''}">Vrienden <b>${server.friends.length||''}</b></button><button data-v11-friends-tab="requests" class="${v11FriendsTab==='requests'?'active':''}">Verzoeken ${req?`<b>${req}</b>`:''}</button><button data-v11-friends-tab="battles" class="${v11FriendsTab==='battles'?'active':''}">Battles ${battles?`<b>${battles}</b>`:''}</button></div>${v11FriendsContent()}`)}

async function v10OpenFriendProfile(id){let r;try{r=await apiProfile11('friend_profile',{userId:id})}catch(e){return toast(e.message)}const p=r.profile,s=r.stats||{};const badges=[p.streak>=7?'🔥 Streak Master':null,p.wins>=3?'⚔️ Battle Beast':null,s.knowledge>=10?'📚 Knowledge':null,p.points>=100?'🏆 Century':null,s.activities>=25?'💪 Consistent':null].filter(Boolean);const page=document.createElement('div');page.className='v10-fullscreen v10-friend-profile v11-friend-profile';page.innerHTML=`<div class="v10-screen-head"><button class="v10-back" data-close>‹</button><span>VRIENDPROFIEL</span><i></i></div><div class="v10-profile-hero">${v10Avatar(p,'xl')}<span>@${esc(p.handle)}</span><h1>${esc(p.name)}</h1><p>${v11Status(p)}</p><div class="v10-profile-chips"><b>🏆 Level ${p.level||1}</b><b>✨ ${p.xp||0} XP</b><b>🔥 ${p.streak||0}</b><b>⭐ ${p.points||0}</b></div></div><div class="v11-profile-grid"><div><b>${s.activities||0}</b><span>Activiteiten</span></div><div><b>${s.knowledge||0}</b><span>Kennis</span></div><div><b>${p.wins||0}</b><span>Battles gewonnen</span></div><div><b>${s.battles||0}</b><span>Battles gespeeld</span></div></div><section class="v10-profile-section"><span>BADGES</span><div class="v10-badges">${badges.length?badges.map(x=>`<b>${esc(x)}</b>`).join(''):'<p>Nog geen badges ontgrendeld.</p>'}</div></section><div class="v10-profile-actions"><button id="v10-prof-chat">💬 Bericht sturen</button><button id="v10-prof-battle">⚔️ Daag uit</button></div>`;document.body.appendChild(page);document.body.classList.add('v10-modal-open');const close=()=>{page.remove();document.body.classList.remove('v10-modal-open')};page.querySelector('[data-close]').onclick=close;page.querySelector('#v10-prof-chat').onclick=()=>{close();v10OpenChat(id,p.name)};page.querySelector('#v10-prof-battle').onclick=()=>{close();v10OpenBattleSetup(p)}}

function v10OpenBattleSetup(friend=null){if(!friend){if(!server.friends.length)return toast('Voeg eerst een vriend toe.');const w=openSheet(`<div class="sheet-top"><div><h2>Nieuwe battle</h2><p class="lead">Wie durf je uit te dagen?</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="v10-pick-friend">${server.friends.map(p=>`<button data-v10-pick="${p.id}">${v10Avatar(p,'sm')}<div><b>${esc(p.name)}</b><span>Level ${p.level||1} · 🔥 ${p.streak||0}</span></div><i>⚔️</i></button>`).join('')}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-v10-pick]').forEach(b=>b.onclick=()=>{const p=server.friends.find(x=>x.id===b.dataset.v10Pick);w.remove();v10OpenBattleSetup(p)});return}let metric='points',duration=168,customDuration=false;const page=document.createElement('div');page.className='v10-fullscreen v10-battle-setup';page.innerHTML=`<div class="v10-screen-head"><button class="v10-back" data-close>‹</button><span>⚔️ BATTLE</span><i></i></div><div class="v10-battle-vs">${v10Avatar(server.v10.me||server.profile,'lg')}<div><strong>VS</strong><small>challenge</small></div>${v10Avatar(friend,'lg')}</div><div class="v10-vs-names"><b>Jij</b><b>${esc(friend.name)}</b></div><section><span class="v10-step">01 · Kies doel</span><div class="v10-option-grid metric">${V10_METRICS.map(([k,n,d,i])=>`<button data-metric="${k}" class="${k===metric?'active':''}"><em>${i}</em><b>${n}</b><small>${d}</small></button>`).join('')}</div><div id="v10-custom-fields" class="v10-custom-fields" hidden><input id="v10-custom-label" maxlength="80" placeholder="Bijv. 5x hardlopen"><input id="v10-custom-target" type="number" min="1" max="500" value="5"></div></section><section><span class="v10-step">02 · Kies duur</span><div class="v10-option-grid durations">${[[24,'24 uur'],[72,'3 dagen'],[168,'7 dagen'],[336,'14 dagen']].map(([h,n])=>`<button data-duration="${h}" class="${h===duration?'active':''}"><b>${n}</b><small>${h===24?'Snel & intens':'Samen volhouden'}</small></button>`).join('')}<button id="v11-custom-duration"><b>Aangepast</b><small>1–31 dagen</small></button></div><div id="v11-custom-duration-box" class="v11-custom-duration-box" hidden><label>Aantal dagen<input id="v11-custom-days" type="number" min="1" max="31" value="5"></label></div></section><button id="v10-send-battle" class="v10-battle-cta">Daag ${esc(friend.name.split(' ')[0])} uit <span>→</span></button>`;document.body.appendChild(page);document.body.classList.add('v10-modal-open');page.querySelector('[data-close]').onclick=()=>{page.remove();document.body.classList.remove('v10-modal-open')};page.querySelectorAll('[data-metric]').forEach(b=>b.onclick=()=>{metric=b.dataset.metric;page.querySelectorAll('[data-metric]').forEach(x=>x.classList.toggle('active',x===b));page.querySelector('#v10-custom-fields').hidden=metric!=='custom'});page.querySelectorAll('[data-duration]').forEach(b=>b.onclick=()=>{duration=Number(b.dataset.duration);customDuration=false;page.querySelector('#v11-custom-duration-box').hidden=true;page.querySelectorAll('[data-duration],#v11-custom-duration').forEach(x=>x.classList.toggle('active',x===b))});page.querySelector('#v11-custom-duration').onclick=()=>{customDuration=true;page.querySelector('#v11-custom-duration-box').hidden=false;page.querySelectorAll('[data-duration],#v11-custom-duration').forEach(x=>x.classList.toggle('active',x.id==='v11-custom-duration'))};page.querySelector('#v10-send-battle').onclick=async()=>{const btn=page.querySelector('#v10-send-battle');btn.disabled=true;try{if(customDuration)duration=Math.max(24,Math.min(744,Number(page.querySelector('#v11-custom-days').value||5)*24));const payload={userId:friend.id,metric,durationHours:duration};if(metric==='custom'){payload.customLabel=page.querySelector('#v10-custom-label').value.trim();payload.customTarget=Number(page.querySelector('#v10-custom-target').value||5)}const r=await api10('battle_create',payload);page.remove();document.body.classList.remove('v10-modal-open');v10BattleSent(friend,r.battle);await loadSocial();render()}catch(e){toast(e.message);btn.disabled=false}}}

const v11SocialBindBase=bind;
bind=function(){v11SocialBindBase();document.querySelectorAll('[data-v11-friends-tab]').forEach(b=>b.onclick=()=>{v11FriendsTab=b.dataset.v11FriendsTab;render()});document.querySelectorAll('[data-v11-profile-card]').forEach(x=>x.onclick=e=>{if(e.target.closest('button'))return;v10OpenFriendProfile(x.dataset.v11ProfileCard)});document.querySelectorAll('[data-v11-message]').forEach(b=>b.onclick=e=>{e.stopPropagation();v10OpenChat(b.dataset.v11Message,b.dataset.v11Name)});document.querySelectorAll('[data-v11-challenge]').forEach(b=>b.onclick=e=>{e.stopPropagation();const p=server.friends.find(x=>x.id===b.dataset.v11Challenge);if(p)v10OpenBattleSetup(p)})}
