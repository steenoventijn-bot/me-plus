/* me+ v12 — World core */
const MW_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-world-api';
const mw={data:null,ownerId:null,loading:false,error:'',edit:false,selectedKey:null,selectedItem:null,zoom:.88,panX:0,panY:28,heartbeat:null,pointers:new Map(),dragStart:null,dragMoved:false,avatar:{x:500,y:365},intro:false};
function apiWorld(op,data={}){return call(MW_API,op,data)}
function mwGlobeIcon(){return `<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M3.5 9h17M3.5 15h17M12 3c2.6 2.4 4 5.4 4 9s-1.4 6.6-4 9c-2.6-2.4-4-5.4-4-9s1.4-6.6 4-9Z"/></svg>`}
const mwNavBase=nav;
nav=function(){const attention=Math.max(server.extras.attention||0,server.stats?.requestCount||0)+(server.stats?.unreadMessages||0);return `<nav class="nav mw-nav">${[['today','today','Vandaag'],['knowledge','knowledge','Kennis'],['world','world','Wereld'],['social','social','Vrienden'],['profile','profile','Profiel']].map(([k,i,l])=>`<button data-nav="${k}" class="${view===k?'active':''}">${k==='world'?mwGlobeIcon():svg(i)}${k==='social'&&attention?`<span class="nav-badge">${attention>9?'9+':attention}</span>`:''}${l}</button>`).join('')}</nav>`}
const mwRenderBase=render;
render=function(){if(view!=='world')return mwRenderBase();const app=el('app');if(!app)return;app.innerHTML=mwWorldView();bind();requestAnimationFrame(()=>{mwBindViewport();mwPaintScene();if(mw.data&&mw.data.isOwner&&!mw.data.world.intro_seen&&!mw.intro)setTimeout(mwShowIntro,180)})}
function mwDayPart(){const h=new Date().getHours();return h<6?'night':h<9?'dawn':h<18?'day':h<21?'evening':'night'}
function mwIso(x,y){return{x:500+(Number(x)-Number(y))*19,y:125+(Number(x)+Number(y))*10}}
function mwWorldView(){if(mw.loading&&!mw.data)return shell(`<section class="mw-loading"><div class="mw-loading-globe">${mwGlobeIcon()}</div><h1>Mijn Wereld</h1><p>Je wereld wordt geladen…</p><div class="mw-load-line"><i></i></div></section>`);if(mw.error&&!mw.data)return shell(`<section class="mw-loading"><h1>Wereld niet beschikbaar</h1><p>${esc(mw.error)}</p><button id="mw-retry" class="button primary">Opnieuw proberen</button></section>`);if(!mw.data){setTimeout(()=>mwLoad(null),0);return shell(`<section class="mw-loading"><div class="mw-loading-globe">${mwGlobeIcon()}</div><h1>Mijn Wereld</h1><p>Even landen…</p></section>`)}const d=mw.data,o=d.owner,w=d.world,s=d.stats;return shell(`<section class="mw-shell ${mwDayPart()} ${d.isOwner?'owner':'visitor'}"><div class="mw-hud"><div class="mw-ownerline">${d.isOwner?'':`<button id="mw-back-own" class="mw-circle">‹</button>`}${v10Avatar(o,'xs')}<div><span>${d.isOwner?'JOUW WERELD':'WERELD VAN'}</span><b>${esc(w.world_name||o.name+' World')}</b></div></div><div class="mw-hud-stats"><i><b>Lv. ${w.world_level}</b><span>World</span></i><i><b>${w.coins}</b><span>Coins</span></i><i><b>${s.battleWins}</b><span>Trophies</span></i></div><button id="mw-menu" class="mw-circle">•••</button></div><div class="mw-viewport" id="mw-viewport"><div class="mw-sky"><i class="cloud c1"></i><i class="cloud c2"></i><i class="bird b1"></i><i class="bird b2"></i></div><div class="mw-scene" id="mw-scene"></div><div class="mw-zoom"><button data-mw-zoom="out">−</button><button data-mw-zoom="in">＋</button></div>${mw.edit&&d.isOwner?`<div class="mw-edit-badge">Bewerkmodus · tik op een object of leeg vak</div>`:''}</div>${d.isOwner?`<div class="mw-toolbar"><button id="mw-house"><span>⌂</span><b>Huis</b></button><button id="mw-edit" class="${mw.edit?'active':''}"><span>✎</span><b>${mw.edit?'Klaar':'Bewerken'}</b></button><button id="mw-collection"><span>◇</span><b>Collectie</b></button><button id="mw-friends"><span>◎</span><b>Vrienden</b></button></div>`:`<div class="mw-toolbar visitor"><button id="mw-like" class="${d.liked?'active':''}"><span>♡</span><b>${d.liked?'Geliked':'Like'} · ${s.likes}</b></button><button id="mw-wave"><span>⌁</span><b>Zwaai</b></button><button id="mw-message"><span>◌</span><b>Bericht</b></button><button id="mw-battle"><span>⚔</span><b>Battle</b></button></div>`}${mw.edit&&d.isOwner?mwEditDock():''}</section>`)}
async function mwLoad(userId=null,force=false){const owner=userId||server.profile?.id;if(!owner)return;if(mw.loading)return;if(!force&&mw.data&&mw.ownerId===owner){mwStartHeartbeat();return}mw.ownerId=owner;mw.loading=true;mw.error='';if(view==='world')render();try{const before=mw.data?.world?.coins||0;const r=await apiWorld('get_world',{userId:owner});mw.data=r;mw.ownerId=owner;mw.loading=false;mw.error='';if(r.isOwner&&before&&r.world.coins>before)toast(`+${r.world.coins-before} World Coins`);mwStartHeartbeat();if(view==='world')render()}catch(e){mw.loading=false;mw.error=e.message;if(view==='world')render()}}
function mwStartHeartbeat(){clearInterval(mw.heartbeat);if(view!=='world'||!mw.ownerId)return;mw.heartbeat=setInterval(()=>{if(document.hidden||view!=='world')return;apiWorld('heartbeat',{userId:mw.ownerId}).catch(()=>{})},30000)}
function mwStopHeartbeat(){clearInterval(mw.heartbeat);mw.heartbeat=null}
function mwGridSvg(){let lines='';for(let i=0;i<=20;i++){const a=mwIso(i,0),b=mwIso(i,20),c=mwIso(0,i),d=mwIso(20,i);lines+=`<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}"/><line x1="${c.x}" y1="${c.y}" x2="${d.x}" y2="${d.y}"/>`}return `<svg class="mw-ground-svg" viewBox="0 0 1000 700" preserveAspectRatio="none"><defs><linearGradient id="mwgrass" x1="0" x2="1" y1="0" y2="1"><stop offset="0" stop-color="#9de28f"/><stop offset="1" stop-color="#6fcf83"/></linearGradient><filter id="mwshadow"><feGaussianBlur stdDeviation="8"/></filter></defs><ellipse cx="500" cy="355" rx="385" ry="205" fill="rgba(15,65,66,.18)" filter="url(#mwshadow)"/><polygon points="500,105 900,315 500,530 100,315" fill="url(#mwgrass)" stroke="rgba(37,112,83,.24)" stroke-width="3"/><g class="mw-grid-lines">${lines}</g><path class="mw-path" d="M500 492 500 323 600 270M500 323 420 280"/><ellipse class="mw-lake" cx="720" cy="330" rx="72" ry="34"/><path class="mw-water-line" d="M667 329c18-8 35 8 53 0s35 8 53 0M680 342c14-7 28 7 42 0s28 7 42 0"/></svg>`}
function mwHouseMarkup(h={}){const lvl=Math.max(1,Number(h.level||1));return `<div class="mw-house level-${lvl} roof-${esc(h.roof||'red')} walls-${esc(h.walls||'wood')} door-${esc(h.door||'oak')} windows-${esc(h.windows||'classic')}"><i class="chimney"></i><div class="roof"><span></span></div><div class="body"><i class="window left"></i><i class="door"></i><i class="window right"></i>${lvl>=2?'<i class="upper-window"></i>':''}</div><div class="porch"></div></div>`}
function mwObjectArt(key){if(key.includes('tree'))return `<div class="mw-tree ${key==='tree_streak7'?'special':''}"><i class="trunk"></i><b></b><b></b><b></b>${key==='tree_streak7'?'<em>7</em>':''}</div>`;if(key==='flowers_pink')return `<div class="mw-flowers">${Array.from({length:6},(_,i)=>`<i style="--i:${i}"></i>`).join('')}</div>`;if(key==='bush_round')return '<div class="mw-bush"><i></i><i></i><i></i></div>';if(key==='bench_wood')return '<div class="mw-bench"><i></i><b></b></div>';if(key==='lamp_glow')return '<div class="mw-lamp"><i></i><b></b></div>';if(key==='pond_small')return '<div class="mw-pond"><i></i><i></i></div>';if(key==='campfire')return '<div class="mw-campfire"><i></i><b></b><b></b></div>';if(key==='fountain')return '<div class="mw-fountain"><i></i><b></b></div>';if(key==='knowledge_house')return '<div class="mw-mini-building knowledge"><b>ME+</b><i></i><span></span></div>';if(key==='growth_zone')return '<div class="mw-mini-building growth"><b>+</b><i></i><span></span></div>';if(key==='trophy_battle')return '<div class="mw-trophy"><i></i><b></b></div>';if(key==='social_flag')return '<div class="mw-flag"><i></i><b></b></div>';if(key==='knowledge_stack')return '<div class="mw-books"><i></i><i></i><i></i></div>';if(key==='golden_duck')return '<div class="mw-golden-duck"><i></i><b></b></div>';return '<div class="mw-rock"></div>'}
function mwSceneHtml(){const d=mw.data;if(!d)return'';const h=mwIso(10,9);const items=(d.items||[]).map(it=>{const p=mwIso(it.grid_x,it.grid_y);return `<button class="mw-object ${mw.selectedItem===it.id?'selected':''}" data-mw-item="${it.id}" style="left:${p.x}px;top:${p.y}px;--rot:${it.rotation||0}deg">${mwObjectArt(it.item_key)}</button>`}).join('');const ownerPos=mwIso(10,11),mePos={x:mw.avatar.x,y:mw.avatar.y};const ownerAvatar=`<div class="mw-character owner-char ${d.isOwner?'me-char':''}" id="${d.isOwner?'mw-player-me':'mw-player-owner'}" style="left:${d.isOwner?mePos.x:ownerPos.x}px;top:${d.isOwner?mePos.y:ownerPos.y}px">${v10Avatar(d.owner,'md')}<span>${esc(d.owner.name.split(' ')[0])}</span></div>`;const selfVisitor=!d.isOwner?`<div class="mw-character me-char" id="mw-player-me" style="left:${mePos.x}px;top:${mePos.y}px">${v10Avatar(server.v10?.me||server.profile,'md')}<span>Jij</span></div>`:'';const others=(d.visitors||[]).filter(v=>v.id!==server.profile?.id&&v.id!==d.owner.id).slice(0,5).map((v,i)=>{const p=mwIso(7+i*2,14-(i%2)*2);return `<div class="mw-character visitor-char" style="left:${p.x}px;top:${p.y}px">${v10Avatar(v,'sm')}<span>${esc(v.name.split(' ')[0])}</span></div>`}).join('');return `${mwGridSvg()}<div class="mw-house-wrap" style="left:${h.x}px;top:${h.y}px">${mwHouseMarkup(d.world.house)}</div>${items}${ownerAvatar}${selfVisitor}${others}<div class="mw-butterfly bf1"></div><div class="mw-butterfly bf2"></div>`}
function mwPaintScene(){const scene=el('mw-scene');if(!scene||!mw.data)return;scene.innerHTML=mwSceneHtml();mwApplyCamera();scene.querySelectorAll('[data-mw-item]').forEach(b=>b.addEventListener('click',e=>{if(!mw.edit||!mw.data.isOwner)return;e.stopPropagation();mw.selectedItem=b.dataset.mwItem;mw.selectedKey=null;render()}))}
function mwApplyCamera(){const scene=el('mw-scene');if(scene)scene.style.transform=`translate(${mw.panX}px,${mw.panY}px) scale(${mw.zoom})`}
function mwEditDock(){const selected=(mw.data?.items||[]).find(x=>x.id===mw.selectedItem);return `<div class="mw-edit-dock"><button id="mw-open-items"><span>＋</span><b>Item plaatsen</b></button>${selected?`<button id="mw-move-hint"><span>↔</span><b>Verplaatsen</b></button><button id="mw-rotate"><span>↻</span><b>Draaien</b></button><button id="mw-remove"><span>×</span><b>Opbergen</b></button>`:'<div class="mw-edit-tip">Selecteer een object om het te verplaatsen.</div>'}</div>`}