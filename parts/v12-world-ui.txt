/* me+ v12 — World interactions */
function mwLocalPoint(clientX,clientY){const scene=el('mw-scene');if(!scene)return null;const r=scene.getBoundingClientRect();if(!r.width||!r.height)return null;return{x:(clientX-r.left)/r.width*1000,y:(clientY-r.top)/r.height*700}}
function mwGridFromPoint(p){if(!p)return null;const a=(p.x-500)/19,b=(p.y-125)/10;const x=Math.round((a+b)/2),y=Math.round((b-a)/2);if(x<0||x>19||y<0||y>19)return null;return{x,y}}
function mwOccupied(x,y,exclude=null){if(x>=8&&x<=12&&y>=7&&y<=11)return true;return (mw.data?.items||[]).some(it=>it.id!==exclude&&Number(it.grid_x)===x&&Number(it.grid_y)===y)}
async function mwGroundTap(ev){if(mw.dragMoved)return;const p=mwLocalPoint(ev.clientX,ev.clientY);if(!p)return;if(mw.edit&&mw.data?.isOwner){const g=mwGridFromPoint(p);if(!g)return toast('Plaats het object op het eiland.');if(mwOccupied(g.x,g.y,mw.selectedItem))return toast('Daar staat al iets. Kies een vrij vak.');try{if(mw.selectedItem){const it=mw.data.items.find(x=>x.id===mw.selectedItem);if(!it)return;await apiWorld('move',{itemId:it.id,x:g.x,y:g.y,rotation:it.rotation||0});it.grid_x=g.x;it.grid_y=g.y;mw.selectedItem=null;hapticV8();render();toast('Object verplaatst ✓')}else if(mw.selectedKey){const r=await apiWorld('place',{itemKey:mw.selectedKey,x:g.x,y:g.y,rotation:0});mw.data=r;mw.selectedKey=null;hapticV8();render();toast('Geplaatst ✓')}}catch(e){toast(e.message)}return}const player=el('mw-player-me');if(!player)return;const x=Math.max(145,Math.min(855,p.x)),y=Math.max(160,Math.min(500,p.y));mw.avatar={x,y};player.style.left=x+'px';player.style.top=y+'px';player.classList.remove('walk');void player.offsetWidth;player.classList.add('walk');hapticV8()}
function mwBindViewport(){const vp=el('mw-viewport'),scene=el('mw-scene');if(!vp||!scene)return;mwApplyCamera();vp.querySelectorAll('[data-mw-zoom]').forEach(b=>b.onclick=e=>{e.stopPropagation();mw.zoom=Math.max(.62,Math.min(1.45,mw.zoom+(b.dataset.mwZoom==='in'?.12:-.12)));mwApplyCamera()});let lastDist=0,lastCenter=null;vp.onpointerdown=e=>{if(e.target.closest('.mw-zoom,.mw-toolbar,.mw-edit-dock,.mw-hud,.mw-object'))return;vp.setPointerCapture?.(e.pointerId);mw.pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});mw.dragMoved=false;mw.dragStart={x:e.clientX,y:e.clientY,panX:mw.panX,panY:mw.panY};if(mw.pointers.size===2){const a=[...mw.pointers.values()];lastDist=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y);lastCenter={x:(a[0].x+a[1].x)/2,y:(a[0].y+a[1].y)/2}}};vp.onpointermove=e=>{if(!mw.pointers.has(e.pointerId))return;mw.pointers.set(e.pointerId,{x:e.clientX,y:e.clientY});if(mw.pointers.size===1&&mw.dragStart){const dx=e.clientX-mw.dragStart.x,dy=e.clientY-mw.dragStart.y;if(Math.abs(dx)+Math.abs(dy)>6)mw.dragMoved=true;mw.panX=Math.max(-310,Math.min(310,mw.dragStart.panX+dx));mw.panY=Math.max(-180,Math.min(190,mw.dragStart.panY+dy));mwApplyCamera()}else if(mw.pointers.size===2){mw.dragMoved=true;const a=[...mw.pointers.values()],dist=Math.hypot(a[0].x-a[1].x,a[0].y-a[1].y),center={x:(a[0].x+a[1].x)/2,y:(a[0].y+a[1].y)/2};if(lastDist){mw.zoom=Math.max(.62,Math.min(1.45,mw.zoom*(dist/lastDist)));mw.panX=Math.max(-310,Math.min(310,mw.panX+(center.x-(lastCenter?.x||center.x))));mw.panY=Math.max(-180,Math.min(190,mw.panY+(center.y-(lastCenter?.y||center.y))));mwApplyCamera()}lastDist=dist;lastCenter=center}};const up=e=>{const wasMoved=mw.dragMoved;mw.pointers.delete(e.pointerId);if(!mw.pointers.size){mw.dragStart=null;lastDist=0;lastCenter=null;if(!wasMoved&&!e.target.closest('.mw-object,.mw-house-wrap,.mw-zoom,.mw-hud,.mw-toolbar,.mw-edit-dock'))mwGroundTap(e);setTimeout(()=>mw.dragMoved=false,0)}};vp.onpointerup=up;vp.onpointercancel=up;vp.onwheel=e=>{e.preventDefault();mw.zoom=Math.max(.62,Math.min(1.45,mw.zoom+(e.deltaY<0?.07:-.07)));mwApplyCamera()}}
function mwCatalogCard(c,inv,unlocked){const q=inv.get(c.item_key)||0,on=unlocked.has(c.item_key);return `<div class="mw-shop-card ${on?'':'locked'}"><div class="mw-shop-art">${mwObjectArt(c.item_key)}</div><div><b>${esc(c.name)}</b><span>${esc(c.rarity)} · World Lv. ${c.min_world_level}</span></div><strong>${q}×</strong>${q>0?`<button data-mw-use="${c.item_key}">Plaats</button>`:on&&c.price>0?`<button data-mw-buy="${c.item_key}">${c.price} coins</button>`:`<small>${on?'Verdiend item':'Nog vergrendeld'}</small>`}</div>`}
function mwOpenItems(){if(!mw.data?.isOwner)return;const w=openSheet(`<div class="sheet-top"><div><h2>Wereld bouwen</h2><p class="lead">Gebruik verdiende items of koop vrijgespeelde decoratie met World Coins.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="mw-shop-balance"><span>World Coins</span><b id="mw-shop-coins">${mw.data.world.coins}</b></div><div id="mw-shop-list"></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();const paint=()=>{const inv=new Map((mw.data.inventory||[]).map(x=>[x.item_key,Number(x.quantity||0)])),unlocked=new Set((mw.data.unlocks||[]).map(x=>x.item_key)),cats=['nature','decor','building','trophy','special'],names={nature:'Natuur',decor:'Decoratie',building:'Gebouwen',trophy:'Trophies',special:'Speciaal'};w.querySelector('#mw-shop-coins').textContent=mw.data.world.coins;w.querySelector('#mw-shop-list').innerHTML=cats.map(cat=>{const list=(mw.data.catalog||[]).filter(c=>c.category===cat);return list.length?`<section class="mw-shop-section"><span>${names[cat]}</span><div>${list.map(c=>mwCatalogCard(c,inv,unlocked)).join('')}</div></section>`:''}).join('');w.querySelectorAll('[data-mw-use]').forEach(b=>b.onclick=()=>{mw.selectedKey=b.dataset.mwUse;mw.selectedItem=null;mw.edit=true;w.remove();render();toast('Tik op een vrij stuk gras om te plaatsen.')});w.querySelectorAll('[data-mw-buy]').forEach(b=>b.onclick=async()=>{b.disabled=true;try{mw.data=await apiWorld('buy',{itemKey:b.dataset.mwBuy});hapticV8();paint();toast('Item toegevoegd aan je collectie')}catch(e){toast(e.message);b.disabled=false}})};paint()}
function mwOpenHouse(){if(!mw.data?.isOwner)return;const h={...mw.data.world.house};const w=openSheet(`<div class="sheet-top"><div><h2>Huis aanpassen</h2><p class="lead">House Level ${h.level||1} groeit automatisch mee met je World Level.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="mw-house-preview" id="mw-house-preview">${mwHouseMarkup(h)}</div><div class="mw-house-options">${[['roof','Dak',['red','blue','green','purple','gold']],['walls','Muren',['wood','stone','modern','colorful']],['door','Deur',['oak','blue','round','modern']],['windows','Ramen',['classic','wide','round','modern']]].map(([k,l,vals])=>`<section><span>${l}</span><div>${vals.map(v=>`<button data-house-key="${k}" data-house-val="${v}" class="${h[k]===v?'active':''}">${v}</button>`).join('')}</div></section>`).join('')}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-house-key]').forEach(b=>b.onclick=async()=>{const key=b.dataset.houseKey,val=b.dataset.houseVal;b.closest('div').querySelectorAll('button').forEach(x=>x.classList.toggle('active',x===b));h[key]=val;w.querySelector('#mw-house-preview').innerHTML=mwHouseMarkup(h);mw.data.world.house={...mw.data.world.house,...h};mwPaintScene();try{const r=await apiWorld('house_update',{[key]:val});mw.data.world.house=r.house;hapticV8()}catch(e){toast(e.message)}})}
function mwOpenCollection(){if(!mw.data)return;const placed=new Map();(mw.data.items||[]).forEach(x=>placed.set(x.item_key,(placed.get(x.item_key)||0)+1));const unlock=new Set((mw.data.unlocks||[]).map(x=>x.item_key)),catalog=mw.data.catalog||[];const specials=catalog.filter(c=>['trophy','special','building'].includes(c.category)&&unlock.has(c.item_key));const w=openSheet(`<div class="sheet-top"><div><h2>Collectie</h2><p class="lead">Alles hier komt uit jouw echte Me+-voortgang.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="mw-collection-stats"><div><b>${unlock.size}</b><span>ontgrendeld</span></div><div><b>${mw.data.items?.length||0}</b><span>geplaatst</span></div><div><b>${mw.data.stats?.battleWins||0}</b><span>battle wins</span></div></div><div class="mw-collect-grid">${specials.length?specials.map(c=>`<div><span>${mwObjectArt(c.item_key)}</span><b>${esc(c.name)}</b><small>${placed.get(c.item_key)||0} geplaatst</small></div>`).join(''):'<p class="empty">Je eerste speciale collectible verschijnt zodra je een mijlpaal haalt.</p>'}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove()}
async function mwOpenFriends(){try{await loadSocial()}catch{}const w=openSheet(`<div class="sheet-top"><div><h2>Bezoek een wereld</h2><p class="lead">Iedere vriend heeft zijn eigen permanente plek.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="mw-world-friends">${server.friends.length?server.friends.map(p=>`<button data-mw-visit="${p.id}">${v10Avatar(p,'sm')}<div><b>${esc(p.name)}</b><span>@${esc(p.handle)}</span></div><i>Bezoek →</i></button>`).join(''):'<div class="empty">Voeg eerst een vriend toe.</div>'}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-mw-visit]').forEach(b=>b.onclick=()=>{w.remove();mwVisitWorld(b.dataset.mwVisit)})}
function mwOpenMenu(){if(!mw.data)return;const own=mw.data.isOwner,w=openSheet(`<div class="sheet-top"><div><h2>${own?'Wereldinstellingen':'Wereld'}</h2><p class="lead">${own?'Bepaal wie jouw plek mag bezoeken.':'Je bent te gast in deze wereld.'}</p></div><button class="icon-btn" data-close>${svg('close')}</button></div>${own?`<div class="setting"><div class="setting-main"><div class="setting-title">Wie mag mijn wereld bezoeken?</div><div class="setting-sub">Bezoekers kunnen niets verplaatsen of verwijderen.</div></div><select id="mw-privacy"><option value="everyone" ${mw.data.world.world_privacy==='everyone'?'selected':''}>Iedereen</option><option value="friends" ${mw.data.world.world_privacy==='friends'?'selected':''}>Alleen vrienden</option><option value="nobody" ${mw.data.world.world_privacy==='nobody'?'selected':''}>Niemand</option></select></div>`:''}<button id="mw-reset-camera" class="button line full">Camera centreren</button>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelector('#mw-reset-camera').onclick=()=>{mw.panX=0;mw.panY=28;mw.zoom=.88;mwApplyCamera();w.remove()};w.querySelector('#mw-privacy')?.addEventListener('change',async e=>{try{const r=await apiWorld('privacy',{privacy:e.target.value});mw.data.world.world_privacy=r.privacy;toast('Wereldprivacy opgeslagen ✓')}catch(err){toast(err.message)}})}
function mwShowIntro(){if(mw.intro||!mw.data?.isOwner||mw.data.world.intro_seen)return;mw.intro=true;const x=document.createElement('div');x.className='mw-intro';x.innerHTML=`<div class="mw-intro-card"><div class="mw-intro-world">${mwGlobeIcon()}</div><span>ME+ WORLD</span><h1>Welkom in jouw wereld.</h1><p>Alles wat je in Me+ bereikt, kan hier zichtbaar worden. Bouw rustig. Ontdek. Maak hem van jou.</p><div class="mw-intro-steps"><i>🏡 <b>Jouw huis</b></i><i>✨ <b>Verdien items</b></i><i>◎ <b>Bezoek vrienden</b></i></div><button>Stap mijn wereld in</button></div>`;document.body.appendChild(x);el('mw-scene')?.classList.add('intro-pan');x.querySelector('button').onclick=async()=>{x.classList.add('leave');el('mw-scene')?.classList.remove('intro-pan');mw.data.world.intro_seen=true;apiWorld('intro_seen').catch(()=>{});setTimeout(()=>x.remove(),350)};setTimeout(()=>x.classList.add('ready'),50)}
async function mwVisitWorld(id){mw.edit=false;mw.selectedKey=mw.selectedItem=null;mw.data=null;mw.error='';mw.ownerId=id;view='world';render();await mwLoad(id,true)}
function mwAddVisitToFriendProfile(id){const page=[...document.querySelectorAll('.v10-friend-profile')].pop();if(!page||page.querySelector('#mw-visit-profile'))return;const actions=page.querySelector('.v10-profile-actions');if(!actions)return;const b=document.createElement('button');b.id='mw-visit-profile';b.innerHTML='🌍 Bezoek wereld';b.onclick=()=>{page.remove();document.body.classList.remove('v10-modal-open');mwVisitWorld(id)};actions.appendChild(b)}
const mwFriendProfileBase=v10OpenFriendProfile;
v10OpenFriendProfile=async function(id){await mwFriendProfileBase(id);mwAddVisitToFriendProfile(id)}
const mwSettingsBase=openGeneralSettings;
openGeneralSettings=function(){mwSettingsBase();const w=[...document.querySelectorAll('.sheet-wrap')].pop();if(!w||w.querySelector('.mw-settings-row'))return;const section=document.createElement('div');section.className='section mw-settings-row';section.innerHTML=`<div class="section-head"><div><h2>Mijn Wereld</h2><p>Wie mag jouw wereld bezoeken?</p></div></div><div class="setting"><div class="setting-main"><div class="setting-title">Wereldbezoek</div><div class="setting-sub">Bezoekers kunnen alleen kijken en reageren.</div></div><select id="mw-settings-privacy"><option value="everyone">Iedereen</option><option value="friends">Alleen vrienden</option><option value="nobody">Niemand</option></select></div>`;const save=w.querySelector('#settings-save')?.closest('.section');(save||w.querySelector('.sheet')).insertAdjacentElement(save?'beforebegin':'beforeend',section);const s=section.querySelector('#mw-settings-privacy');s.value=mw.data?.isOwner?mw.data.world.world_privacy:'friends';s.onchange=async()=>{try{await apiWorld('privacy',{privacy:s.value});if(mw.data?.isOwner)mw.data.world.world_privacy=s.value;toast('Wereldprivacy opgeslagen ✓')}catch(e){toast(e.message)}}}
const mwBindBase=bind;
bind=function(){mwBindBase();const worldBtn=document.querySelector('[data-nav="world"]');if(worldBtn)worldBtn.onclick=()=>{mwStopHeartbeat();mw.ownerId=server.profile?.id||null;mw.data=null;mw.edit=false;view='world';render();mwLoad(server.profile?.id,true)};document.querySelectorAll('[data-nav]').forEach(b=>{if(b.dataset.nav!=='world')b.addEventListener('click',mwStopHeartbeat)});if(view!=='world')return;el('mw-retry')?.addEventListener('click',()=>mwLoad(mw.ownerId,true));el('mw-back-own')?.addEventListener('click',()=>mwVisitWorld(server.profile.id));el('mw-menu')?.addEventListener('click',mwOpenMenu);el('mw-house')?.addEventListener('click',mwOpenHouse);el('mw-collection')?.addEventListener('click',mwOpenCollection);el('mw-friends')?.addEventListener('click',mwOpenFriends);el('mw-edit')?.addEventListener('click',()=>{mw.edit=!mw.edit;mw.selectedItem=mw.selectedKey=null;render()});el('mw-open-items')?.addEventListener('click',mwOpenItems);el('mw-move-hint')?.addEventListener('click',()=>toast('Tik op een vrij vak om dit object te verplaatsen.'));el('mw-rotate')?.addEventListener('click',async()=>{const it=mw.data.items.find(x=>x.id===mw.selectedItem);if(!it)return;const rot=(Number(it.rotation||0)+90)%360;try{await apiWorld('move',{itemId:it.id,x:it.grid_x,y:it.grid_y,rotation:rot});it.rotation=rot;hapticV8();mwPaintScene()}catch(e){toast(e.message)}});el('mw-remove')?.addEventListener('click',async()=>{const id=mw.selectedItem;if(!id)return;try{await apiWorld('remove',{itemId:id});mw.selectedItem=null;await mwLoad(mw.ownerId,true);toast('Terug in je inventory ✓')}catch(e){toast(e.message)}});el('mw-like')?.addEventListener('click',async()=>{try{const r=await apiWorld('like',{userId:mw.ownerId});mw.data.liked=r.liked;mw.data.stats.likes=r.likes;hapticV8();render()}catch(e){toast(e.message)}});el('mw-wave')?.addEventListener('click',async()=>{try{await apiWorld('wave',{userId:mw.ownerId});hapticV8();toast(`Je zwaait naar ${mw.data.owner.name.split(' ')[0]} 👋`)}catch(e){toast(e.message)}});el('mw-message')?.addEventListener('click',()=>v10OpenChat(mw.data.owner.id,mw.data.owner.name));el('mw-battle')?.addEventListener('click',()=>v10OpenBattleSetup(mw.data.owner))}