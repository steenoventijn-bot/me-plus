/* The 3D world uses the existing account, catalog and social actions. */
const W14_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-world-v14-api';
let w14Module=null,w14Import=null,w14Stage=null,w14LoadSequence=0,w14MountSequence=0,w14SaveBusy=false;
function w14Engine(){return w14Module?Promise.resolve(w14Module):(w14Import||(w14Import=import('./world/renderer.js').then(m=>w14Module=m).catch(e=>{w14Import=null;throw e})))}
function w14Own(){return server.profile?.id||null}
function w14Hint(text,valid=true){const hint=el('w14-hint');if(hint){hint.textContent=text;hint.classList.toggle('invalid',!valid)}}
function w14ItemName(key){return (mw.data?.catalog||[]).find(x=>x.item_key===key)?.name||({house_main:'Je huis',pond_garden:'Tuinvijver',dock_wood:'Houten steiger'})[key]||'Object'}
function w14Select(id){mw.selectedItem=id;w14Stage?.select(id);w14Dock();}
function w14Dock(){const dock=el('w14-dock');if(!dock)return;const it=mw.data?.items?.find(x=>x.id===mw.selectedItem);dock.innerHTML=`<button id="w14-items"><span>＋</span>Item plaatsen</button>${it?`<div class="w14-selected"><strong>${esc(w14ItemName(it.item_key))}</strong><span>Houd vast en sleep</span></div><button id="w14-rotate" aria-label="Object draaien">↻</button>${it.item_key==='house_main'?'<button id="w14-house-selected">Aanpassen</button>':'<button id="w14-store" aria-label="Object opbergen">Opbergen</button>'}`:'<p>Houd een object vast en sleep het naar een vrije plek.</p>'}`;el('w14-items').onclick=mwOpenItems;el('w14-house-selected')?.addEventListener('click',mwOpenHouse);el('w14-rotate')?.addEventListener('click',async()=>{const p=w14Module.itemPosition(it),q={key:it.item_key,id:it.id,rotation:(Number(it.rotation||0)+90)%360,...p},check=w14Module.placementResult(q,mw.data.items);if(!check.valid)return toast(check.reason);await w14SavePlacement(q)});el('w14-store')?.addEventListener('click',async()=>{if(w14SaveBusy)return;w14SaveBusy=true;try{await apiWorld('remove',{itemId:it.id});await mwLoad(w14Own(),true);toast('Object opgeborgen')}catch(e){toast(e.message)}finally{w14SaveBusy=false}})}
async function w14SavePlacement(q){if(w14SaveBusy)return toast('Even wachten, je vorige wijziging wordt opgeslagen.');if(!mw.data?.isOwner)return;const account=w14Own(),worldId=mw.ownerId;w14SaveBusy=true;w14Hint('Opslaan…');try{const r=await call(W14_API,q.id?'move':'place',{itemId:q.id,itemKey:q.key,x:q.x,y:q.y,rotation:q.rotation});if(w14Own()!==account||mw.ownerId!==worldId)return;const index=mw.data.items.findIndex(i=>i.id===r.item.id);if(index<0)mw.data.items.push(r.item);else mw.data.items[index]=r.item;if(r.inventory)mw.data.inventory=r.inventory;mw.selectedItem=r.item.id;mw.selectedKey=null;mwPaintScene();w14Dock();w14Hint('Opgeslagen ✓');hapticV8()}catch(e){w14Hint(e.message,false);toast(e.message);await mwLoad(worldId,true)}finally{w14SaveBusy=false}}
// Latest request wins, including friend → own transitions while a request is pending.
mwLoad=async function(userId=null,force=false){const own=w14Own(),owner=userId||own;if(!owner)return;mw.myWorldId=own;mw.viewedWorldId=owner;if(!force&&mw.data?.owner?.id===owner&&!mw.loading){mw.ownerId=owner;mwStartHeartbeat();return}const seq=++w14LoadSequence;mw.ownerId=owner;mw.loading=true;mw.error='';if(mw.data?.owner?.id!==owner)mw.data=null;if(view==='world')render();try{const r=await apiWorld('get_world',{userId:owner});if(seq!==w14LoadSequence||w14Own()!==own)return;if(r.owner?.id!==owner)throw new Error('Deze wereld kon niet worden geladen.');mw.data=r;mw.ownerId=owner;mw.loading=false;if(r.isOwner&&server.profile?.avatarConfig)r.owner={...r.owner,avatarConfig:server.profile.avatarConfig};mwStartHeartbeat();if(view==='world')render()}catch(e){if(seq!==w14LoadSequence)return;mw.loading=false;mw.error=e.message;if(view==='world')render()}};
mwVisitWorld=async function(id){mw.edit=false;mw.selectedItem=mw.selectedKey=null;view='world';await mwLoad(id||w14Own(),true)};
q6ReturnOwnWorld=function(){return mwVisitWorld(w14Own())};
const w14PreviousRender=render;
render=function(){if(view!=='world'){w14MountSequence++;w14Stage?.pause(true);return w14PreviousRender()}const app=el('app');if(!app)return;const d=mw.data,seq=++w14MountSequence;if(!d){app.innerHTML=`<main class="w14-loading"><div class="w14-mark">me<span>+</span></div><h1>${mw.error?'Nog niet geland':'Jouw wereld'}</h1><p>${esc(mw.error||'We maken je eiland klaar…')}</p>${mw.error?'<button id="w14-retry" class="button primary">Opnieuw proberen</button>':'<span class="w14-load-track"></span>'}${mw.viewedWorldId!==w14Own()?'<button id="w14-loading-home" class="button line">Terug naar mijn wereld</button>':''}</main>${nav()}`;bind();el('w14-retry')?.addEventListener('click',()=>mwLoad(mw.ownerId,true));el('w14-loading-home')?.addEventListener('click',q6ReturnOwnWorld);if(!mw.loading&&!mw.error)void mwLoad(mw.ownerId||w14Own());return}
 app.innerHTML=`<main class="w14-world ${mw.edit?'editing':''}"><div class="w14-stage" id="w14-stage"></div><header class="w14-hud">${d.isOwner?'':'<button id="w14-home" aria-label="Verlaat vriendenwereld">‹</button>'}<div class="w14-title"><span>${d.isOwner?'JOUW WERELD':'OP BEZOEK'}</span><h1>${esc(d.world.world_name||d.owner.name+'’s World')}</h1></div><div class="w14-stat"><b>Lv. ${Number(d.world.world_level||1)}</b><small>World</small></div><div class="w14-stat"><b><span class="w14-coin">●</span> ${Number(d.world.coins||0)}</b><small>Coins</small></div><button id="w14-menu" aria-label="Wereldmenu">•••</button></header><div id="w14-players" class="w14-players"></div><div class="w14-zoom"><button id="w14-zoom-out" aria-label="Uitzoomen">−</button><button id="w14-zoom-in" aria-label="Inzoomen">＋</button></div><div class="w14-bottom"><p id="w14-hint" class="w14-hint" aria-live="polite">${mw.edit?'Bewerkmodus · houd vast en sleep':d.isOwner?'Een kleine wereld. Helemaal van jou.':'Je bent op bezoek bij '+esc(d.owner.name.split(' ')[0])}</p><div class="w14-toolbar">${d.isOwner?`<button id="w14-house"><span>⌂</span>Huis</button><button id="w14-edit" class="${mw.edit?'active':''}"><span>✎</span>${mw.edit?'Klaar':'Bewerken'}</button><button id="w14-collection"><span>◇</span>Collectie</button><button id="w14-friends"><span>◎</span>Vrienden</button>`:`<button id="w14-like"><span>♡</span>${d.liked?'Geliked':'Like'} · ${Number(d.stats?.likes||0)}</button><button id="w14-wave"><span>⌁</span>Zwaai</button><button id="w14-message"><span>◌</span>Bericht</button><button id="w14-leave"><span>⌂</span>Verlaten</button>`}</div>${mw.edit&&d.isOwner?'<div class="w14-dock" id="w14-dock"></div>':''}</div></main>${nav()}`;
 bind();el('w14-home')?.addEventListener('click',q6ReturnOwnWorld);el('w14-leave')?.addEventListener('click',q6ReturnOwnWorld);el('w14-house')?.addEventListener('click',mwOpenHouse);el('w14-edit')?.addEventListener('click',()=>{mw.edit=!mw.edit;mw.selectedItem=null;render()});el('w14-collection')?.addEventListener('click',mwOpenCollection);el('w14-friends')?.addEventListener('click',mwOpenFriends);el('w14-menu').onclick=w14Menu;el('w14-zoom-out').onclick=()=>w14Stage?.setZoom(-.15);el('w14-zoom-in').onclick=()=>w14Stage?.setZoom(.15);el('w14-message')?.addEventListener('click',()=>v10OpenChat(d.owner.id,d.owner.name));el('w14-wave')?.addEventListener('click',async()=>{try{await apiWorld('wave',{userId:d.owner.id});toast('Je zwaait naar '+d.owner.name.split(' ')[0])}catch(e){toast(e.message)}});el('w14-like')?.addEventListener('click',async()=>{try{const r=await apiWorld('like',{userId:d.owner.id});d.liked=r.liked;d.stats.likes=r.likes;render()}catch(e){toast(e.message)}});w14Dock();
 w14Engine().then(m=>{if(seq!==w14MountSequence||view!=='world')return;const host=el('w14-stage');if(w14Stage)w14Stage.mount(host);else w14Stage=m.createWorld(host,{onSelect:w14Select,onDrop:w14SavePlacement,onHint:w14Hint,onCameraChange:w14Players,onError:text=>w14Hint(text,false)});w14Stage.pause(false);w14Stage.setEdit(mw.edit);w14Stage.update(mw.data);w14Players()}).catch(e=>{if(seq!==w14MountSequence)return;el('w14-stage').innerHTML='<div class="w14-render-error"><h2>3D kon niet starten</h2><p>Sluit de app en probeer opnieuw. Gebruik een browser met WebGL2.</p></div>';console.error('world renderer',e)})
};
mwPaintScene=function(){if(w14Stage&&mw.data){w14Stage.update(mw.data);w14Stage.setEdit(mw.edit);w14Players()}};
mwApplyCamera=function(){w14Stage?.reset()};
mwBindViewport=function(){};
function w14Players(){const node=el('w14-players');if(!node||!mw.data||!w14Stage)return;const d=mw.data,people=[d.owner,...(!d.isOwner?[server.profile]:[]),...(d.visitors||[]).filter(p=>p.id!==d.owner.id&&p.id!==w14Own()).slice(0,3)];node.innerHTML=people.map((p,i)=>{const q=w14Stage.project(8+i*1.1,12.3+(i%2),.45);return `<div class="w14-player" style="left:${q.x}px;top:${q.y}px">${v10Avatar(p,'sm')}<span>${esc(p.name.split(' ')[0])}</span></div>`}).join('')}
function w14Menu(){const own=mw.data?.isOwner,w=openSheet(`<div class="sheet-top"><h2>Mijn Wereld</h2><button class="icon-btn" data-close>${svg('close')}</button></div><div class="w14-menu">${own?'<button id="w14-menu-house">Huis aanpassen</button>':'<button id="w14-menu-home">Terug naar mijn wereld</button>'}<button id="w14-center">Camera centreren</button>${own?`<label>Wie mag je wereld bezoeken?<select id="w14-privacy"><option value="friends">Alleen vrienden</option><option value="everyone">Iedereen</option><option value="nobody">Niemand</option></select></label>`:''}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelector('#w14-menu-house')?.addEventListener('click',()=>{w.remove();mwOpenHouse()});w.querySelector('#w14-menu-home')?.addEventListener('click',()=>{w.remove();q6ReturnOwnWorld()});w.querySelector('#w14-center').onclick=()=>{w14Stage?.reset();w14Players();w.remove()};const select=w.querySelector('#w14-privacy');if(select){select.value=mw.data.world.world_privacy;select.onchange=async()=>{try{const r=await apiWorld('privacy',{privacy:select.value});mw.data.world.world_privacy=r.privacy;toast('Privacy opgeslagen')}catch(e){toast(e.message)}}}}
// Real hold → drag from the inventory; taps remain available for choosing an item.
mwOpenItems=function(){if(!mw.data?.isOwner)return;document.querySelector('.mw-build-panel')?.remove();const panel=document.createElement('section');panel.className='mw-build-panel w14-build';panel.innerHTML='<div class="mw-build-head"><div><span>MAAK HET VAN JOU</span><h2>Op je eiland</h2></div><button data-close aria-label="Sluiten">×</button></div><p>Houd de sleepgreep vast en sleep het item naar het gras.</p><div class="w14-item-list"></div>';document.body.appendChild(panel);let hold;const paint=()=>{const inv=new Map((mw.data.inventory||[]).map(i=>[i.item_key,Number(i.quantity)])),unlocked=new Set((mw.data.unlocks||[]).map(i=>i.item_key));panel.querySelector('.w14-item-list').innerHTML=(mw.data.catalog||[]).filter(c=>!['house_main'].includes(c.item_key)).map(c=>{const qty=inv.get(c.item_key)||0,canBuy=unlocked.has(c.item_key)&&Number(c.price)>0;return `<div class="w14-item-card"><button class="w14-item-grab" data-key="${esc(c.item_key)}" ${qty?'':'disabled'}><span>${esc(c.name)}</span><small>${qty?qty+' beschikbaar':canBuy?'Nog niet in je voorraad':'Nog niet vrijgespeeld'}</small><span class="w14-drag-handle" aria-hidden="true">⠿</span></button>${canBuy?`<button data-buy="${esc(c.item_key)}">${Number(c.price)} coins</button>`:''}</div>`}).join('');panel.querySelectorAll('[data-key]').forEach(b=>{const handle=b.querySelector('.w14-drag-handle');handle.onpointerdown=e=>{if(b.disabled||e.button!==0)return;e.preventDefault();handle.setPointerCapture(e.pointerId);const start={x:e.clientX,y:e.clientY};hold=setTimeout(()=>{panel.style.visibility='hidden';w14Stage?.beginInventoryDrag(b.dataset.key,e);const show=()=>{panel.remove();document.removeEventListener('pointerup',show);document.removeEventListener('pointercancel',show)};document.addEventListener('pointerup',show);document.addEventListener('pointercancel',show)},320);handle.onpointermove=ev=>{if(Math.hypot(ev.clientX-start.x,ev.clientY-start.y)>10)clearTimeout(hold)};handle.onpointerup=handle.onpointercancel=()=>clearTimeout(hold)}});panel.querySelectorAll('[data-buy]').forEach(b=>b.onclick=async()=>{b.disabled=true;try{mw.data=await apiWorld('buy',{itemKey:b.dataset.buy});paint();toast('Item toegevoegd')}catch(e){toast(e.message);b.disabled=false}})};panel.querySelector('[data-close]').onclick=()=>{clearTimeout(hold);panel.remove()};paint()};
mwOpenHouse=async function(){if(!mw.data?.isOwner)return;const owner=w14Own(),original=structuredClone(mw.data.world.house||{}),engine=await w14Engine(),draft={...engine.houseDefaults,...original};let active='style',preview=null,saving=false;const wrap=openSheet(`<div class="w14-house-editor"><header><div><span>JOUW HUIS</span><h2>Maak het eigen</h2></div><button data-close aria-label="Sluiten">×</button></header><div class="w14-house-layout"><aside><div id="w14-house-tabs"></div><div id="w14-house-choices"></div></aside><div class="w14-house-stage"><div id="w14-house-preview"></div><div class="w14-house-turn"><button id="w14-turn-left" aria-label="Huis linksom bekijken">↶</button><span>Je hele huis in beeld</span><button id="w14-turn-right" aria-label="Huis rechtsom bekijken">↷</button></div></div></div><footer><button id="w14-house-cancel" class="button line">Annuleren</button><button id="w14-house-save" class="button primary">Opslaan</button></footer></div>`);wrap.classList.add('w14-house-sheet');w14Stage?.pause(true);const close=()=>{if(saving)return;preview?.dispose();wrap.remove();w14Stage?.pause(false)};const paint=()=>{wrap.querySelector('#w14-house-tabs').innerHTML=Q6_HOUSE_OPTIONS.map(([key,label])=>`<button data-tab="${key}" class="${active===key?'active':''}">${label}</button>`).join('');const group=Q6_HOUSE_OPTIONS.find(x=>x[0]===active);wrap.querySelector('#w14-house-choices').innerHTML=`<h3>${group[1]}</h3>`+group[2].map(([val,name])=>`<button data-choice="${val}" class="${draft[active]===val?'active':''}">${name}<span>${draft[active]===val?'✓':''}</span></button>`).join('');wrap.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{active=b.dataset.tab;paint()});wrap.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>{draft[active]=b.dataset.choice;preview?.update(draft);paint()})};paint();preview=engine.createHousePreview(wrap.querySelector('#w14-house-preview'),draft);wrap.querySelector('[data-close]').onclick=close;wrap.querySelector('#w14-house-cancel').onclick=close;wrap.querySelector('#w14-turn-left').onclick=()=>preview.rotate(-.45);wrap.querySelector('#w14-turn-right').onclick=()=>preview.rotate(.45);wrap.querySelector('#w14-house-save').onclick=async()=>{if(saving)return;saving=true;const btn=wrap.querySelector('#w14-house-save');btn.disabled=true;btn.textContent='Opslaan…';try{const r=await q6Api('save_house',{house:draft});if(owner===w14Own()&&mw.data?.isOwner){mw.data.world.house=r.house;mwPaintScene()}saving=false;close();toast('Huis opgeslagen ✓')}catch(e){saving=false;btn.disabled=false;btn.textContent='Opslaan';toast(e.message)}}};

const w14BindPrevious=bind;
bind=function(){w14BindPrevious();const world=document.querySelector('[data-nav="world"]');if(world)world.onclick=()=>mwVisitWorld(w14Own());document.querySelectorAll('[data-nav]').forEach(b=>{if(b.dataset.nav!=='world')b.addEventListener('click',()=>{w14LoadSequence++;mw.loading=false;w14Stage?.pause(true)})})};

// Navigation paints cached account data immediately; server refresh never blocks the tap.
let w14NavigationSequence=0;
const w14NavRequests=new Map(),w14NavFresh=new Map();
function w14Navigate(target,filter){
 const seq=++w14NavigationSequence,account=server.profile?.id;
 view=target;if(filter)knowledgeFilter=filter;
 render();
 if(target!=='profile'&&target!=='social')return Promise.resolve();
 const key=account+':'+target;
 if(Date.now()-(w14NavFresh.get(key)||0)<30000)return Promise.resolve();
 let request=w14NavRequests.get(key);
 if(!request){request=Promise.resolve().then(()=>target==='profile'?refreshBase():loadSocial('people')).then(()=>{w14NavFresh.set(key,Date.now())}).finally(()=>{w14NavRequests.delete(key)});w14NavRequests.set(key,request)}
 return request.then(()=>{if(seq===w14NavigationSequence&&view===target&&server.profile?.id===account){const y=window.scrollY||0;render();window.scrollTo(0,y)}}).catch(e=>console.warn('navigation refresh',e));
}
