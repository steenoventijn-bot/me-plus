/* Together on an adventure: clear daily actions and server-authoritative cooperation. */
const W18_API='https://npolnflektojripucyvq.supabase.co/functions/v1/me-plus-adventure-api';
const w18Kinds={together:{name:'Samen vooruit',icon:'☀',target:20,text:'Verdien samen 20 punten met activiteiten en goede kennisquizzen.'},activity:{name:'In beweging',icon:'✦',target:10,text:'Rond samen 10 verbeteractiviteiten met een bewijsfoto af.'},knowledge:{name:'Ontdekkingsreis',icon:'✧',target:10,text:'Beantwoord samen 10 kennisquizzen goed.'}};
let w18Teams=[],w18Account=null,w18Loaded=0,w18Loading=null,w18Close=null;
function w18Api(op,data={}){return call(W18_API,op,data)}
function w18Day(){try{return JSON.parse(localStorage.getItem('meplus-day:'+server.profile?.id+':'+localISO())||'{}')}catch{return {}}}
function w18Mark(key){if(!server.profile?.id)return;try{localStorage.setItem('meplus-day:'+server.profile.id+':'+localISO(),JSON.stringify({...w18Day(),[key]:true}))}catch{}}
const w18Call=call;
call=async function(base,op,data={}){const account=server.profile?.id,result=await w18Call(base,op,data);if(account!==server.profile?.id)return result;if(op==='complete_activity'){w18Mark('activity');w18Loaded=0}if(base===K14_API&&op==='answer'){w18Mark('learn');w18Loaded=0}if(['send_message_v10','wave','battle_create'].includes(op)||base===W18_API&&['create','accept'].includes(op))w18Mark('connect');return result};
function w18Add(title=''){openEvent(null,'growth');const input=el('ev-title');if(input){input.value=title;if(!title)input.focus()}const sheet=input?.closest('.sheet');sheet?.classList.add('w18-activity-sheet');const heading=sheet?.querySelector('h2');if(heading)heading.textContent='Jouw volgende goede stap';const saveButton=el('save-event');if(saveButton)saveButton.textContent='Activiteit toevoegen'}
function w18BoatArt(){return `<svg viewBox="0 0 260 160" aria-hidden="true"><circle cx="208" cy="38" r="25" fill="#ffd776"/><path d="M0 110Q35 94 65 109T130 107T195 108T260 104V160H0Z" fill="#79dedc"/><path d="M0 135Q35 119 65 133T130 131T195 134T260 129V160H0Z" fill="#35a8c6"/><path d="M70 109H186L167 135H93Z" fill="#f5755e"/><path d="M117 35V111" stroke="#2c5570" stroke-width="5" stroke-linecap="round"/><path d="M124 40L173 98H124Z" fill="#fff7df"/><path d="M110 60L82 100H110Z" fill="#ffc651"/><path d="M121 33L150 39L121 47Z" fill="#f5755e"/><path d="M91 141H164" stroke="#e5fcf6" stroke-width="4" stroke-linecap="round"/></svg>`}
function w18Mission(label,description,key,icon,done){return `<button class="w18-mission ${done?'done':''}" data-w18-step="${key}"><i>${done?'✓':icon}</i><span><b>${label}</b><small>${description}</small></span><em>${done?'Klaar':'→'}</em></button>`}
todayView=function(){
 const p=server.profile||{},date=localISO(),items=eventsFor(date),done=items.filter(e=>state.done[doneKey(e,date)]).length,day=w18Day(),activity=day.activity||items.some(e=>e.type==='growth'&&state.done[doneKey(e,date)]),steps=[activity,day.learn,day.connect].filter(Boolean).length,article=server.articles[0];
 const intro=!localStorage.getItem('meplus-welcome18:'+p.id),invites=w18Teams.filter(t=>t.expedition?.status==='pending'&&t.expedition.inviter_id!==p.id).length;
 return shell(`<div class="w18-today"><header class="w18-greeting"><div><span>${esc(fmtDate())}</span><h1>${greeting()}, ${esc((p.name||state.profileName||'jij').split(' ')[0])}<i>✦</i></h1><p>Een kleine stap vandaag. Een grotere wereld morgen.</p></div><b class="w18-streak">🔥 ${server.extras?.streak||0}</b></header>
 <button class="w18-add" id="w18-add"><i>＋</i><span><small>MAAK RUIMTE VOOR JEZELF</small><strong>Voeg een activiteit toe</strong><em>Bewegen, ontspannen of iets nieuws proberen</em></span><b>↗</b></button>
 <div class="w18-suggestions"><span>Een idee nodig?</span>${['10 minuten wandelen','Even rekken','Iets creatiefs doen'].map(x=>`<button data-w18-suggest="${x}">${x} +</button>`).join('')}</div>
 <section class="w18-day"><div class="w18-section-heading"><div><span>JOUW RICHTING</span><h2>Drie fijne stappen</h2></div><b>${steps}/3</b></div><div class="w18-three-progress">${[0,1,2].map(i=>`<i class="${i<steps?'on':''}"></i>`).join('')}</div>${w18Mission('Doe iets voor jezelf',activity?'Een activiteit afgerond':'Rond een activiteit af','activity','✦',activity)}${w18Mission('Ontdek iets nieuws',day.learn?'Je hebt je kennis getest':'Lees & doe de quiz','learn','✧',day.learn)}${w18Mission('Maak contact',day.connect?'Samen is leuker':'Zoek een vriend op','connect','♡',day.connect)}</section>
 <section class="w18-plan"><div class="w18-section-heading"><div><span>VANDAAG OP JE LIJST</span><h2>${items.length?'Jouw planning':'Wat wil je vandaag doen?'}</h2></div><button id="w18-appointment">＋ Afspraak</button></div>${items.length?`<p>${done} van ${items.length} afgerond</p><div class="list">${items.map(e=>eventRow(e,date)).join('')}</div>`:'<div class="w18-plan-empty"><span>☀</span><p>Je dag hoeft niet vol. Eén activiteit is een mooi begin.</p><button data-w18-step="activity">Kies mijn eerste activiteit →</button></div>'}</section>
 <button class="button line full" data-nav="knowledge">Kennis voor vandaag · 3 teksten →</button>
 <div class="w18-small-stats"><span>⭐ <b>${p.points||0}</b> punten</span><span>🌱 <b>${server.extras?.streak||0}</b> dagen reeks</span><span>🏡 <b>Lv. ${p.level||1}</b></span></div></div>`)
};
async function w18LoadHub(force=false){
 const account=server.profile?.id;if(!account)return;if(w18Account!==account){w18Teams=[];w18Loaded=0;w18Loading=null;w18Account=account}
 if(!force&&Date.now()-w18Loaded<30000)return;if(w18Loading)return force?w18Loading.then(()=>w18LoadHub(true)):w18Loading;
 const request=w18Api('hub').then(r=>{if(server.profile?.id===account){w18Teams=r.teams||[];w18Loaded=Date.now()}}).finally(()=>{if(w18Loading===request)w18Loading=null});w18Loading=request;return request;
}
async function w18OpenHarbour(){
 w18Close?.();const account=server.profile?.id;if(!account)return;
 const page=document.createElement('div');page.className='w18-harbour';page.setAttribute('role','dialog');page.setAttribute('aria-modal','true');page.setAttribute('aria-label','Samen op avontuur');
 page.innerHTML='<header><button data-close aria-label="Sluiten">‹</button><div><small>JOUW VOLGENDE BESTEMMING</small><h1>De haven</h1></div><button data-refresh aria-label="Vernieuwen">↻</button></header><main><p role="status">Je bemanning ophalen…</p></main>';
 document.body.appendChild(page);w14Stage?.pause(true);let closed=false,scene=null,selected=null,busy=false,kind='together',timer=null,paintSequence=0;
 const close=()=>{closed=true;paintSequence++;clearInterval(timer);scene?.dispose();scene=null;page.remove();w18Close=null;if(view==='world')w14Stage?.pause(false)};w18Close=close;
 page.querySelector('[data-close]').onclick=close;page.addEventListener('keydown',e=>{if(e.key==='Escape')close()});
 const main=page.querySelector('main');
 const perform=async(op,data)=>{if(busy)return;busy=true;main.querySelectorAll('button').forEach(b=>b.disabled=true);try{const t=await w18Api(op,data);if(closed||account!==server.profile?.id)return;selected=t.id;await w18LoadHub(true);paint()}catch(e){if(!closed){toast(e.message);paint()}}finally{busy=false}};
 const paint=()=>{
  if(closed||account!==server.profile?.id)return close();const seq=++paintSequence;scene?.dispose();scene=null;
  const team=w18Teams.find(t=>t.id===selected);
  if(team){
   const e=team.expedition,k=w18Kinds[e?.kind]||w18Kinds.together,other=team.members.find(m=>m.id!==account),pending=e?.status==='pending',active=e?.status==='active',complete=e?.status==='completed',ratio=Math.min(100,100*team.score/(e?.target||1));
   main.innerHTML=`<button class="w18-back-text" data-back>‹ Alle bestemmingen</button><section class="w18-crew-heading"><span>JIJ + ${esc(other?.name||'JE VRIEND')}</span><h2>${esc(k.name)}</h2><p>${esc(k.text)} Jullie hebben 7 dagen vanaf accepteren. Alleen nieuwe verdiende punten tellen. Iedereen draagt bij op zijn eigen tempo.</p></section>
   <div class="w18-crew">${team.members.map(m=>`<div>${v10Avatar(m,'md')}<b>${esc(m.id===account?'Jij':m.name)}</b><span>${m.score} bijdrage${m.score===1?'':'n'}</span></div>`).join('')}</div>
   ${pending?`<section class="w18-invite"><h3>${e.inviter_id===account?'Je uitnodiging is verstuurd':'Ga je mee op avontuur?'}</h3><p>Na accepteren zien jullie elkaars expeditietotaal. Foto’s volgen je eigen deelinstellingen. Samen de finish halen geeft ieder 25 coins en bij je eerste expeditie een lantaarn voor je eiland.</p>${e.inviter_id!==account?'<button data-accept class="w18-primary">Ja, ik vaar mee →</button><button data-decline>Liever niet</button>':'<button data-decline>Uitnodiging intrekken</button>'}</section>`:`<section class="w18-route"><div><b>${complete?'Bestemming bereikt! ✨':active?'Onderweg naar jullie doel':e?.status==='expired'?'Deze reis is afgelopen':'Klaar voor een nieuwe reis'}</b><span>${team.score} / ${e?.target||20}</span></div><div class="w18-route-track"><i style="width:${ratio}%"></i><span style="left:${Math.max(4,Math.min(94,ratio))}%">⛵</span></div><p>${complete?'Ieder 25 coins verdiend. Jullie eiland groeit mee!':active?`Nog ${Math.max(0,Math.ceil((new Date(e.ends_at)-Date.now())/86400000))} dagen. Activiteiten en goede quizantwoorden tellen automatisch mee.`:'Geen punten kwijt. Je kunt samen gewoon opnieuw beginnen.'}</p>${active?`<button data-grow class="w18-primary">${e.kind==='knowledge'?'Lees iets & doe de quiz':'Voeg een activiteit toe'} →</button>`:'<button data-again class="w18-primary">Plan jullie volgende reis →</button>'}</section>`}
   ${!pending||team.completed>0?`<section class="w18-shared"><div class="w18-section-heading"><div><span>JULLIE EIGEN PLEK</span><h3>Het vriendeneiland</h3></div><b>${team.completed} reizen</b></div><div id="w18-shared-scene" aria-label="Jullie gezamenlijk opgebouwde eiland"></div><div class="w18-island-milestones">${[[1,'Bloementuin'],[3,'Clubhuis'],[5,'Samenstrand']].map(([n,label])=>`<span class="${team.completed>=n?'done':''}">${team.completed>=n?'✓':'◌'} ${label}<small>${n} voltooide ${n===1?'reis':'reizen'}</small></span>`).join('')}</div><p>Jullie gezamenlijke resultaten bouwen dit eiland op. Zo blijft iedere bijdrage zichtbaar.</p></section>`:''}
   <section class="w18-connect"><h3>Samen is meer dan punten</h3><button data-visit>🏝 Bezoek ${esc(other?.name||'je vriend')}</button><button data-chat>💬 Geef een compliment</button><button data-photos>▧ Bekijk activiteitenfoto’s</button></section>`;
   main.querySelector('[data-back]').onclick=()=>{selected=null;paint()};main.querySelector('[data-accept]')?.addEventListener('click',()=>perform('accept',{teamId:team.id}));main.querySelector('[data-decline]')?.addEventListener('click',()=>perform('decline',{teamId:team.id}));main.querySelector('[data-again]')?.addEventListener('click',()=>perform('create',{userId:other.id,kind:e.kind}));
   main.querySelector('[data-grow]')?.addEventListener('click',()=>{close();if(e.kind==='knowledge')w14Navigate('knowledge');else{w14Navigate('today');w18Add()}});
   main.querySelector('[data-visit]').onclick=()=>{close();mwVisitWorld(other.id)};main.querySelector('[data-photos]').onclick=()=>{close();w16OpenAlbum('friends',other.id)};
   main.querySelector('[data-chat]').onclick=async()=>{close();await v10OpenChat(other.id,other.name);const input=el('v10-chat-text');if(input){input.value='Lekker bezig! 👏';input.dispatchEvent(new Event('input'));input.focus({preventScroll:true})}};
   const host=main.querySelector('#w18-shared-scene');if(host)w14Engine().then(engine=>{if(closed||seq!==paintSequence)return;scene=engine.createWorld(host,{onError:text=>toast(text)});scene.update(engine.sharedIslandData(team));scene.setEdit(false)}).catch(()=>{if(host.isConnected)host.innerHTML='<p>Het eiland kon niet laden. Tik bovenaan op vernieuwen.</p>'});
   return;
  }
  main.innerHTML=`<section class="w18-harbour-intro"><div><span>SAMEN OP AVONTUUR</span><h2>Een boot. Een vriend.<br>Een nieuw verhaal.</h2><p>Verdien samen vooruitgang en bouw jullie eigen vriendeneiland. Begin klein, vaar samen verder.</p></div>${w18BoatArt()}</section>
  ${w18Teams.length?`<section class="w18-team-list"><h2>Jullie reizen</h2>${w18Teams.map(t=>{const e=t.expedition,other=t.members.find(m=>m.id!==account),status=e?.status==='pending'?(e.inviter_id===account?'Wacht op je vriend':'Uitnodiging · jij bent aan zet'):e?.status==='active'?`${t.score} / ${e.target} · onderweg`:e?.status==='completed'?'Gehaald! Bekijk jullie eiland':'Plan een nieuwe reis';return `<button data-team="${t.id}">${v10Avatar(other,'sm')}<span><b>${esc(other?.name||'Vriend')}</b><small>${esc(status)}</small></span><em>→</em></button>`}).join('')}</section>`:''}
  <section class="w18-new-trip"><span>1 · KIES JULLIE UITDAGING</span><h2>Waar gaan jullie voor?</h2><div class="w18-trip-options">${Object.entries(w18Kinds).map(([key,k])=>`<button data-kind="${key}" aria-pressed="${kind===key}"><i>${k.icon}</i><b>${k.name}</b><small>${k.text}</small></button>`).join('')}</div><span>2 · NODIG EEN VRIEND UIT</span><p>Jullie starten op nul zodra je vriend accepteert. Voortgang wordt automatisch bijgehouden; geen handmatig afvinken.</p><div class="w18-friend-picks">${server.friends.length?server.friends.map(f=>`<button data-invite="${f.id}">${v10Avatar(f,'sm')}<b>${esc(f.name)}</b><span>Uitnodigen →</span></button>`).join(''):'<p>Voeg eerst een vriend toe. Je hoeft dit avontuur niet alleen te doen.</p><button data-find-friend class="w18-primary">Vind een vriend →</button>'}</div></section>`;
  main.querySelectorAll('[data-team]').forEach(b=>b.onclick=()=>{selected=b.dataset.team;paint()});main.querySelectorAll('[data-kind]').forEach(b=>b.onclick=()=>{kind=b.dataset.kind;paint()});main.querySelectorAll('[data-invite]').forEach(b=>b.onclick=()=>perform('create',{userId:b.dataset.invite,kind}));main.querySelector('[data-find-friend]')?.addEventListener('click',()=>{close();w14Navigate('social');v10OpenFriendSearch()});
 };
 const refresh=async()=>{try{await w18LoadHub(true);if(!closed)paint()}catch(e){if(!closed){main.innerHTML='<p>Je haven kon niet laden. Je voortgang blijft bewaard.</p><button data-retry>Opnieuw proberen</button>';main.querySelector('[data-retry]').onclick=refresh}}};page.querySelector('[data-refresh]').onclick=refresh;
 try{const friends=await api('friends');if(!closed&&account===server.profile?.id)server.friends=friends.friends||[]}catch{}
 await refresh();if(closed)return;timer=setInterval(async()=>{if(document.hidden||closed||busy)return;try{const previous=JSON.stringify(w18Teams);await w18LoadHub(true);if(!closed&&JSON.stringify(w18Teams)!==previous){const scroll=page.scrollTop;paint();page.scrollTop=scroll}}catch{}},20000);
}
const w18BindBase=bind;
bind=function(){w18BindBase();el('w18-add')?.addEventListener('click',()=>w18Add());el('w18-appointment')?.addEventListener('click',()=>openEvent(null,'appointment'));document.querySelectorAll('[data-w18-suggest]').forEach(b=>b.onclick=()=>w18Add(b.dataset.w18Suggest));document.querySelectorAll('[data-w18-step]').forEach(b=>b.onclick=()=>{if(b.dataset.w18Step==='activity'){const e=eventsFor(localISO()).find(e=>e.type==='growth'&&!state.done[doneKey(e,localISO())]);if(e)openProof(e,localISO());else w18Add()}else if(b.dataset.w18Step==='learn')w14Navigate('knowledge');else w18OpenHarbour()});el('w18-dismiss')?.addEventListener('click',()=>{localStorage.setItem('meplus-welcome18:'+server.profile.id,'1');render()});for(const id of ['w18-harbour','w18-boat','w18-social-harbour'])el(id)?.addEventListener('click',w18OpenHarbour);
 if(server.profile?.id&&(view==='today'||view==='social'))void w18LoadHub().then(()=>{const b=el('w18-social-harbour');const count=w18Teams.filter(t=>t.expedition?.status==='pending'&&t.expedition.inviter_id!==server.profile.id).length;if(b&&count)b.querySelector('small').textContent=count+' uitnodiging'+(count===1?'':'en')+' voor jou';const home=el('w18-harbour');if(home&&count)home.querySelector('p').textContent=count+' uitnodiging'+(count===1?' wacht':'en wachten')+' op je.'}).catch(()=>{});
};
const w18SocialBase=socialView;
socialView=function(){return w18SocialBase().replace('</section>','</section><button id="w18-social-harbour" class="w18-social-entry"><i>⛵</i><span><b>Samen op avontuur</b><small>Jullie expedities en vriendeneiland</small></span><em>→</em></button>')};

/* Three calm daily picks; the full shelf is an explicit choice. */
let k19More=false,k19Owner=null;
function k19Hash(text){let n=2166136261;for(const c of String(text))n=Math.imul(n^c.charCodeAt(0),16777619);return n>>>0}
function k19Picks(articles,user,date,interests=[]){const list=[...articles].sort((a,b)=>Number(interests.includes(b.category))-Number(interests.includes(a.category))||k19Hash(user+':'+a.id)-k19Hash(user+':'+b.id)||String(a.id).localeCompare(String(b.id)));if(!list.length)return [];const day=Math.floor(Date.parse(date+'T00:00:00Z')/86400000),start=((day*3+k19Hash(user))%list.length+list.length)%list.length;return Array.from({length:Math.min(3,list.length)},(_,i)=>list[(start+i)%list.length])}
function k19Done(){try{return JSON.parse(localStorage.getItem('meplus-quiz-done:'+server.profile?.id)||'[]')}catch{return []}}
const k19Answer=k14Answer;
k14Answer=async function(choice){const s=k14Reader,owner=server.profile?.id;await k19Answer(choice);if(s?.attempt&&server.profile?.id===owner){try{localStorage.setItem('meplus-quiz-done:'+owner,JSON.stringify([...new Set([...k19Done(),s.article.id])]))}catch{}}};
knowledgeView=function(){if(k19Owner!==server.profile?.id){k19More=false;k19Owner=server.profile?.id}const all=server.articles||[],done=k19Done(),picks=k19Picks(all,server.profile?.id||'',localISO(),server.profile?.interests||[]);let items=k19More?all:picks;if(k19More&&knowledgeFilter==='Bewaard')items=items.filter(a=>server.saved.has(a.id));else if(k19More&&knowledgeFilter!=='Alles')items=items.filter(a=>a.category===knowledgeFilter);return shell(`<div class="top"><div><h1 class="title">${k19More?'Meer ontdekken':'Kennis voor vandaag'}</h1><p class="subtitle">${k19More?'Kies wat je leuk vindt.':'Drie teksten. Op jouw tempo.'}</p></div></div>${k19More?`<button class="button line small" id="k19-back">‹ Jouw drie teksten</button><div class="filters">${['Alles','Bewaard',...CATEGORIES].map(f=>`<button class="filter ${knowledgeFilter===f?'active':''}" data-k19-filter="${esc(f)}">${esc(f)}</button>`).join('')}</div>`:''}<div class="knowledge-list k19-list">${items.length?items.map(a=>`<div>${knowledgeCard(a)}${done.includes(a.id)?'<small class="k19-read">✓ Afgerond</small>':''}</div>`).join(''):'<p class="empty">Hier staan nog geen teksten.</p>'}</div>${!k19More?`${picks.length&&picks.every(a=>done.includes(a.id))?'<p class="k19-finished">Klaar voor vandaag 🎉</p>':''}<button class="button line full" id="k19-more">Meer ontdekken →</button>`:''}`)};
const k19Bind=bind;
bind=function(){k19Bind();el('k19-more')?.addEventListener('click',()=>{k19More=true;knowledgeFilter='Alles';render()});el('k19-back')?.addEventListener('click',()=>{k19More=false;render()});document.querySelectorAll('[data-k19-filter]').forEach(b=>b.onclick=()=>{knowledgeFilter=b.dataset.k19Filter;render()})};
const k19Navigate=w14Navigate;
w14Navigate=function(next,filter){if(next==='knowledge'){k19More=!!filter;knowledgeFilter=filter||'Alles'}return k19Navigate(next,filter)};
