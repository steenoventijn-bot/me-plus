function socialView(){const count=server.incomingRequests.length||server.stats?.requestCount||0;return shell(`<div class="top"><div><div class="eyebrow">Samen groeien</div><h1 class="title">Vrienden</h1></div></div><div class="segmented">${[['people',`Vrienden${count?` (${count})`:''}`],['battle','Battle'],['feed','Activiteit'],['messages','Berichten']].map(([k,l])=>`<button class="seg ${socialTab===k?'active':''}" data-social="${k}">${l}</button>`).join('')}</div><div id="social-content">${socialContent()}</div>`)}
function socialContent(){if(socialTab==='people')return `<div class="search"><input id="friend-q" placeholder="Zoek op naam of @gebruikersnaam"><button id="friend-go" class="button primary small">Zoek</button></div><div id="search-results"></div>${server.incomingRequests.length?`<section class="section"><div class="section-head"><div><h2>Verzoeken</h2><p>Mensen die jou willen toevoegen</p></div></div>${server.incomingRequests.map(requestRow).join('')}</section>`:''}${server.outgoingRequests.length?`<section class="section"><div class="section-head"><div><h2>Verstuurd</h2></div></div>${server.outgoingRequests.map(outgoingRow).join('')}</section>`:''}<section class="section"><div class="section-head"><div><h2>Jouw vrienden</h2></div></div>${server.friends.length?server.friends.map(personRow).join(''):'<div class="empty">Nog geen vrienden. Zoek hierboven iemand op.</div>'}</section>`;if(socialTab==='battle')return `<div class="filters" style="margin-top:16px">${[['week','Deze week'],['month','Deze maand'],['all','All time']].map(([k,l])=>`<button class="filter ${socialPeriod===k?'active':''}" data-period="${k}">${l}</button>`).join('')}</div><section class="section">${server.leaderboard.length?server.leaderboard.map((x,i)=>leaderRow(x,i)).join(''):'<div class="empty">Voeg vrienden toe om samen een ranglijst te krijgen.</div>'}</section>`;if(socialTab==='feed')return `<section class="section">${server.feed.length?server.feed.map(feedRow).join(''):'<div class="empty">Nog geen gedeelde activiteiten.</div>'}</section>`;return `<section class="section">${server.conversations.length?server.conversations.map(conversationRow).join(''):'<div class="empty">Nog geen gesprekken. Je kunt berichten sturen zodra jullie vrienden zijn.</div>'}</section>`}
function personRow(p){return `<div class="person"><div class="avatar">${esc(initials(p.name))}</div><div class="person-main"><div class="person-name">${esc(p.name)}</div><div class="person-meta">@${esc(p.handle)} · ${esc(p.rank)} · ${p.points} pt</div></div><button class="button line small" data-chat-user="${p.id}" data-chat-name="${esc(p.name)}">Bericht</button></div>`}
function requestRow(p){return `<div class="request-box"><div class="person" style="padding:0;border:0"><div class="avatar">${esc(initials(p.name))}</div><div class="person-main"><div class="person-name">${esc(p.name)}</div><div class="person-meta">@${esc(p.handle)}</div></div></div><div class="request-actions"><button class="button primary small" data-accept="${p.id}">Accepteren</button><button class="button line small" data-reject="${p.id}">Weigeren</button></div></div>`}
function outgoingRow(p){return `<div class="person"><div class="avatar">${esc(initials(p.name))}</div><div class="person-main"><div class="person-name">${esc(p.name)}</div><div class="person-meta">Verzoek verstuurd</div></div><button class="button line small" data-cancel-request="${p.id}">Intrekken</button></div>`}
function searchRow(p){let action=p.relation==='friend'?'<span class="person-meta">Vriend</span>':p.relation==='outgoing'?`<button class="button line small" data-cancel-request="${p.id}">Verstuurd</button>`:p.relation==='incoming'?`<button class="button primary small" data-accept="${p.id}">Accepteren</button>`:p.canRequest?`<button class="button primary small" data-request="${p.id}">Toevoegen</button>`:'<span class="person-meta">Geen verzoeken</span>';return `<div class="person"><div class="avatar">${esc(initials(p.name))}</div><div class="person-main"><div class="person-name">${esc(p.name)}</div><div class="person-meta">@${esc(p.handle)}${p.points!=null?` · ${p.points} pt`:''}</div></div>${action}</div>`}
function leaderRow(x,i){return `<div class="person"><div class="medal">${i+1}</div><div class="person-main"><div class="person-name">${esc(x.name)}${x.isMe?' · jij':''}</div><div class="person-meta">${esc(x.rank)} · level ${x.level}</div></div><div class="score">${x.score}</div></div>`}
function feedRow(x){return `<div class="feed"><div class="avatar">${esc(initials(x.name))}</div><div><p><b>${esc(x.name)}</b> rondde “${esc(x.title)}” af. <span class="person-meta">+${x.points}</span></p><div class="person-meta">${new Intl.DateTimeFormat('nl-NL',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}).format(new Date(x.created_at))}</div>${x.proofUrl?`<img class="proof" src="${esc(x.proofUrl)}" alt="Gedeeld bewijs">`:''}</div></div>`}
function conversationRow(p){return `<button class="menu-btn" data-chat-user="${p.id}" data-chat-name="${esc(p.name)}"><div class="avatar">${esc(initials(p.name))}</div><div class="menu-main"><div class="menu-title">${esc(p.name)}</div><div class="menu-sub">${esc(p.last?.body||'Start een gesprek')}</div></div><span class="chev">›</span></button>`}

function profileView(){const p=server.profile||{},stats=server.stats||{},nr=nextRank(p.points||0);return shell(`<div class="profile-head"><div class="avatar lg">${esc(initials(p.name||state.profileName))}</div><div><h1>${esc(p.name||state.profileName)}</h1><div class="handle">@${esc(p.handle||'')} · Level ${p.level||1} · ${esc(p.rank||'Starter')}</div></div></div><div class="section" style="margin-top:0"><div class="section-head"><div><h2>${p.points||0} punten</h2><p>${nr?`${nr[1]-(p.points||0)} punten tot ${nr[0]}`:'Hoogste rang bereikt'}</p></div></div><div class="progress"><span style="width:${progress(p)}%"></span></div></div><section class="section"><div class="stats"><div class="stat"><strong>${stats.friendCount||server.friends.length||0}</strong><span>vrienden</span></div><div class="stat"><strong>${stats.activities||0}</strong><span>activiteiten</span></div><div class="stat"><strong>${stats.reads||0}</strong><span>artikelen</span></div></div></section><section class="section"><div class="section-head"><div><h2>Profiel</h2><p>Beheer wat me+ voor jou doet.</p></div></div><div class="menu"><button class="menu-btn" id="general-settings"><span class="menu-icon">${svg('settings')}</span><span class="menu-main"><span class="menu-title">Algemene instellingen</span><span class="menu-sub">Meldingen, interesses, privacy en zichtbaarheid</span></span><span class="chev">›</span></button><button class="menu-btn" id="rank-info"><span class="menu-icon">${svg('ranks')}</span><span class="menu-main"><span class="menu-title">Rangen & punten</span><span class="menu-sub">Bekijk alle levels en hoeveel punten je nodig hebt</span></span><span class="chev">›</span></button><button class="menu-btn" data-nav="knowledge" data-filter-jump="Bewaard"><span class="menu-icon">${svg('bookmark')}</span><span class="menu-main"><span class="menu-title">Bewaarde kennis</span><span class="menu-sub">${server.extras.saved||0} opgeslagen artikelen</span></span><span class="chev">›</span></button><button class="menu-btn"><span class="menu-icon">${svg('flame')}</span><span class="menu-main"><span class="menu-title">Jouw dagreeks</span><span class="menu-sub">${server.extras.streak||0} dagen met echte voortgang</span></span></button></div></section>`)}
