function openGeneralSettings(){const p=server.profile||{};const pushText=Notification.permission==='granted'&&server.push.enabled?'Aan':Notification.permission==='denied'?'Geblokkeerd':'Uit';const w=openSheet(`<div class="sheet-top"><div><h2>Algemene instellingen</h2><p class="lead">Alles wat je eerder op verschillende plekken moest zoeken staat nu hier.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="setting"><div class="setting-main"><div class="setting-title">Meldingen</div><div class="setting-sub" id="push-state">${pushText}. Nodig voor afspraken en het badgegetal op je homescreen.</div></div><button id="push-toggle" class="button ${pushText==='Aan'?'soft':'primary'} small">${pushText==='Aan'?'Opnieuw koppelen':'Inschakelen'}</button></div>${!standalone()?'<div class="notice" style="margin-top:10px">Op iPhone werken pushmeldingen voor me+ wanneer je de app via Safari op je beginscherm hebt gezet en hem vanaf dat icoon opent.</div>':''}<div class="section"><div class="section-head"><div><h2>Interesses</h2><p>Bepaal welke kennis je vaker ziet.</p></div></div><div class="genre-grid" id="settings-interests">${CATEGORIES.map(c=>`<button class="genre ${(p.interests||[]).includes(c)?'selected':''}" data-settings-genre="${esc(c)}">${svg(categoryIcon(c))}<b>${esc(c)}</b></button>`).join('')}</div></div><div class="section"><div class="section-head"><div><h2>Privacy</h2></div></div>${settingToggle('shareTasks','Deel activiteiten','Vrienden kunnen zien welke verbeteractiviteiten je voltooit.',p.shareTasks)}${settingToggle('shareProof','Deel bewijsfoto’s','Alleen bij gedeelde activiteiten en alleen met vrienden.',p.shareProof)}${settingToggle('shareProgress','Deel voortgang','Laat punten, level en rang aan vrienden zien.',p.shareProgress)}<div class="setting"><div class="setting-main"><div class="setting-title">Wie ziet je profiel?</div><div class="setting-sub">Kies of niet-vrienden je voortgang al mogen zien.</div></div><select id="visibility" style="width:auto"><option value="everyone" ${p.profileVisibility==='everyone'?'selected':''}>Iedereen</option><option value="friends" ${p.profileVisibility==='friends'?'selected':''}>Vrienden</option></select></div><div class="setting"><div class="setting-main"><div class="setting-title">Vriendverzoeken</div><div class="setting-sub">Bepaal of anderen je mogen toevoegen.</div></div><select id="requests" style="width:auto"><option value="everyone" ${p.friendRequestPolicy==='everyone'?'selected':''}>Iedereen</option><option value="nobody" ${p.friendRequestPolicy==='nobody'?'selected':''}>Niemand</option></select></div></div><div class="section"><button id="settings-save" class="button primary full">Instellingen opslaan</button></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-settings-genre]').forEach(b=>b.onclick=()=>b.classList.toggle('selected'));w.querySelectorAll('[data-setting]').forEach(b=>b.onclick=()=>b.classList.toggle('on'));w.querySelector('#push-toggle').onclick=async()=>{const b=w.querySelector('#push-toggle');b.disabled=true;try{await enablePush();w.querySelector('#push-state').textContent='Aan. Afspraken kunnen nu ook melden wanneer me+ gesloten is.';b.textContent='Gekoppeld';b.className='button soft small';await syncAllReminders()}catch(e){toast(e.message);b.disabled=false}};w.querySelector('#settings-save').onclick=async()=>{const interests=[...w.querySelectorAll('[data-settings-genre].selected')].map(x=>x.dataset.settingsGenre);try{const r=await api('settings',{interests,shareTasks:w.querySelector('[data-setting="shareTasks"]').classList.contains('on'),shareProof:w.querySelector('[data-setting="shareProof"]').classList.contains('on'),shareProgress:w.querySelector('[data-setting="shareProgress"]').classList.contains('on'),profileVisibility:w.querySelector('#visibility').value,friendRequestPolicy:w.querySelector('#requests').value});server.profile=r.profile;state.knowledgeSeen=true;save();await refreshBase();w.remove();render();toast('Instellingen opgeslagen.')}catch(e){toast(e.message)}}}
function settingToggle(key,title,sub,on){return `<div class="setting"><div class="setting-main"><div class="setting-title">${title}</div><div class="setting-sub">${sub}</div></div><button class="switch ${on?'on':''}" data-setting="${key}" aria-label="${title}"><i></i></button></div>`}
function openRanks(){const points=server.profile?.points||0;const w=openSheet(`<div class="sheet-top"><div><h2>Rangen & punten</h2><p class="lead">Elke 10 echte punten stijg je een rang. Punten kun je niet kopen.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="rank-list">${RANKS.map((r,i)=>`<div class="rank-row"><div class="rank-icon ${points>=r[1]&&(!RANKS[i+1]||points<RANKS[i+1][1])?'current':''}">${svg(r[2])}</div><div><div class="rank-name">${esc(r[0])}</div><div class="rank-level">Level ${i+1}${points>=r[1]?' · bereikt':''}</div></div><div class="rank-points">${r[1]}+ pt</div></div>`).join('')}</div>`);w.querySelector('[data-close]').onclick=()=>w.remove()}

async function registerSW(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register('/sw.js?v=7');reg.update().catch(()=>{})}catch(e){console.warn('sw',e)}}
function b64ToBytes(s){const pad='='.repeat((4-s.length%4)%4),b=(s+pad).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(b),a=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)a[i]=raw.charCodeAt(i);return a}
async function enablePush(){if(!('Notification'in window)||!('serviceWorker'in navigator)||!('PushManager'in window))throw new Error('Pushmeldingen worden op dit apparaat niet ondersteund.');if(!standalone()&&/iPad|iPhone|iPod/.test(navigator.userAgent))throw new Error('Zet me+ eerst via Safari op je beginscherm en open hem vanaf daar.');const perm=await Notification.requestPermission();if(perm!=='granted')throw new Error('Geef me+ toestemming voor meldingen in je telefooninstellingen.');const reg=await navigator.serviceWorker.ready;const cfg=await api7('push_config');let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64ToBytes(cfg.publicKey)});await api7('register_push',{subscription:sub.toJSON()});server.push.enabled=true;return true}

document.addEventListener('visibilitychange',()=>{if(!document.hidden){onAppOpen();syncAllReminders()}});
boot();
})();
