/* me+ v8 focused product layer */
const V8_MOMENTS=[
 'Eén goede keuze is genoeg om je dag een andere richting te geven.',
 'Klein volhouden wint het meestal van groot beginnen.',
 'Vooruitgang hoeft vandaag niet indrukwekkend te zijn. Wel echt.',
 'Maak het jezelf makkelijk om het goede te doen.',
 'Een rustige dag kan nog steeds een sterke dag zijn.',
 'Begin met de stap die je zonder discussie kunt uitvoeren.',
 'Je hoeft niet alles af te krijgen om verder te komen.',
 'Herhaling maakt van een goed idee uiteindelijk een gewoonte.',
 'Kies vandaag liever één duidelijke winst dan vijf halve plannen.',
 'Wat je vaak doet, gaat uiteindelijk zwaarder wegen dan wat je soms doet.'
];
const V8_STREAK_MILESTONES=[3,7,14,30,60,100];

function duckV8(cls=''){return `<svg class="duck-v8 ${cls}" viewBox="0 0 64 64" aria-hidden="true"><path d="M15 38c0-11 8-19 19-19 8 0 14 4 17 10 5 0 9 3 9 7 0 5-5 8-11 8H36c-4 5-10 8-17 8-9 0-15-5-15-11 0-3 2-6 5-8 1 3 3 5 6 5Z"/><circle cx="41" cy="27" r="2.1"/><path d="M51 30l9 3-9 4"/><path d="M25 50c0 4-3 7-7 7M36 49c0 4 3 7 7 7"/></svg>`}
function hapticV8(){try{navigator.vibrate?.(22)}catch{}}
function daySeedV8(){return Number(localISO().replaceAll('-',''))||1}
function momentV8(){return V8_MOMENTS[daySeedV8()%V8_MOMENTS.length]}
function weeklyDoneV8(){const now=new Date(),dow=(now.getDay()+6)%7,start=new Date(now);start.setDate(now.getDate()-dow);start.setHours(0,0,0,0);let total=0;for(const key of Object.keys(state.done||{})){if(!state.done[key])continue;const d=key.split('@')[1];if(!d)continue;const x=new Date(d+'T12:00:00');if(x>=start&&x<=now)total++}return total}
function feedbackV8(){const today=Number(server.stats?.today?.total||0),week=weeklyDoneV8(),streak=Number(server.extras?.streak||0);if(today>=3)return `Sterke dag. Je hebt vandaag al ${today} punten verdiend.`;if(streak>=7)return `Je reeks staat op ${streak} dagen. Dat is inmiddels een patroon.`;if(week>=3)return `Lekker bezig. Je hebt deze week ${week} momenten afgerond.`;if(today>0)return `Je hebt vandaag al echte voortgang geboekt.`;return `Eén afgerond moment is genoeg om vandaag te laten tellen.`}

function launchV8(){if(document.getElementById('v8-launch'))return;const s=document.createElement('div');s.id='v8-launch';s.className='launch-v8';s.innerHTML=`<div class="launch-v8-inner"><div class="launch-v8-mark">me<span>+</span></div><div class="launch-v8-name">me+</div><div class="launch-v8-line"><i></i></div></div>`;document.body.appendChild(s)}
function finishLaunchV8(){const s=document.getElementById('v8-launch');if(!s)return;s.classList.add('leave');setTimeout(()=>s.remove(),420)}
const bootV8Base=boot;
boot=async function(){const started=Date.now();launchV8();await bootV8Base();const left=Math.max(0,1150-(Date.now()-started));setTimeout(()=>{finishLaunchV8();maybeStreakMilestoneV8()},left)};

function nextOccurrenceV8(e){if(!e?.start||!e?.date)return null;let d=new Date(`${e.date}T${e.start}:00`),guard=0;while(guard++<800){const day=localISO(d),done=!!state.done?.[doneKey(e,day)],past=d.getTime()<Date.now()-1000;if(!done&&!past)return d;if(!e.repeat||e.repeat==='none')return null;if(e.repeat==='daily')d.setDate(d.getDate()+1);else if(e.repeat==='weekly')d.setDate(d.getDate()+7);else if(e.repeat==='monthly')d.setMonth(d.getMonth()+1);else return null}return null}
async function syncReminderForEvent(e){const key=`event:${e.id}`;if(!server.push?.enabled||!e.start||e.reminder==='none'){try{await api7('cancel_reminder',{eventKey:key})}catch{}return}const occ=nextOccurrenceV8(e);if(!occ){try{await api7('cancel_reminder',{eventKey:key})}catch{}return}const scheduled=new Date(occ.getTime()-reminderOffset(e.reminder)*60000);if(scheduled.getTime()<Date.now()-30000)return;try{await api7('schedule_reminder',{eventKey:key,title:e.title,kind:e.type==='growth'?'growth':'appointment',repeat:e.repeat||'none',timezone:tz(),scheduledAt:scheduled.toISOString(),leadMinutes:reminderOffset(e.reminder)})}catch(err){console.warn('reminder',err)}}

function eventTypeLabelV8(e){return e.type==='growth'?'verbeteractiviteit':e.type==='task'?'taak':'afspraak'}
function eventRow(e,date){const done=!!state.done[doneKey(e,date)],growth=e.type==='growth';return `<div class="row ${growth?'growth-row':''}">${growth?`<button class="proof-needed ${done?'done':''}" data-growth="${esc(e.id)}" data-date="${date}" aria-label="${done?'Voltooid':'Bewijs toevoegen'}">${done?'✓':duckV8('mini')}</button>`:`<button class="check ${done?'done':''}" data-check="${esc(e.id)}" data-date="${date}">✓</button>`}<div class="row-main" data-edit-event="${esc(e.id)}"><div class="row-title">${esc(e.title||'Activiteit')}</div><div class="row-meta">${e.start?esc(e.start)+' · ':''}${eventTypeLabelV8(e)}${e.repeat&&e.repeat!=='none'?` · ${repeatLabel(e.repeat)}`:''}${e.reminder&&e.reminder!=='none'?` · melding ${reminderLabel(e.reminder)}`:''}</div></div>${growth?`<span class="points plus-points">${done?'verdiend':'+1 Plus-punt'}</span>`:''}</div>`}

function todayView(){const p=server.profile,a=server.articles[0],date=localISO(),items=eventsFor(date),done=items.filter(e=>state.done[doneKey(e,date)]).length,next=nextRank(p?.points||0),streak=Number(server.extras?.streak||0);return shell(`<div class="top today-v8-top"><div class="dayline"><div class="eyebrow">${esc(fmtDate())}</div><h1 class="greeting-v8"><span>${greeting()},</span><span class="full-name-v8">${esc(p?.name||state.profileName)}</span></h1><p>${items.length?`${done} van ${items.length} momenten afgerond.`:'Je dag is nog open. Kies één stap die echt telt.'}</p></div><div class="score-chip"><b>${p?.points||0} pt</b><span>${esc(p?.rank||'Starter')}</span></div></div>${a?`<article class="hero" data-article="${a.id}"><div class="meta">Weetje van vandaag · ${esc(a.category)}</div><h2>${esc(a.title)}</h2><p>${esc(a.intro)}</p><div class="hero-foot"><button class="hero-btn" data-article="${a.id}">Lees verder</button><span class="read-time">${readLabel(a)} · +1 bij echt lezen</span></div></article>`:''}<section class="meplus-moment-v8"><div><span>Jouw Me+ moment</span><p>${esc(momentV8())}</p></div>${duckV8('moment-duck')}</section><section class="section"><div class="section-head"><div><h2>Vandaag</h2><p>Afspraken en verbeteractiviteiten</p></div><button id="quick-add" class="quick-add-v8" aria-label="Snel toevoegen">+</button></div>${items.length?`<div class="list">${items.map(e=>eventRow(e,date)).join('')}</div>`:'<div class="empty">Nog niets gepland. Voeg bijvoorbeeld sporten, lezen of een afspraak toe.</div>'}</section><section class="section"><div class="section-head"><div><h2>Jouw ritme</h2><p>${esc(feedbackV8())}</p></div></div><div class="mini-cards v8-rhythm"><div class="mini"><strong>${server.stats?.today?.total||0}</strong><span>punten vandaag</span></div><div class="mini streak-mini"><strong>🔥 ${streak}</strong><span>${streak===1?'dag':'dagen'} reeks</span></div><div class="mini"><strong>${next?next[1]-(p?.points||0):'✓'}</strong><span>${next?'tot '+next[0]:'hoogste rang'}</span></div></div></section>`)}

function quickActionsV8(){const w=openSheet(`<div class="sheet-top"><div><h2>Wat wil je toevoegen?</h2><p class="lead">Kies de snelste route.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="quick-grid-v8"><button data-quick="appointment"><span>${svg('today')}</span><b>Afspraak</b><small>Plan een moment</small></button><button data-quick="growth"><span>${duckV8('quick-duck')}</span><b>Verbeteractiviteit</b><small>Verdien Plus-punten</small></button><button data-quick="knowledge"><span>${svg('knowledge')}</span><b>Kennis</b><small>Leer iets nieuws</small></button><button data-quick="task"><span>${svg('spark')}</span><b>Taak</b><small>Zonder punten</small></button></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-quick]').forEach(b=>b.onclick=()=>{const type=b.dataset.quick;w.remove();if(type==='knowledge'){view='knowledge';render()}else openEvent(null,type)})}

function openEvent(e=null,presetType=null){const isEdit=!!e,date=e?.date||localISO(),rem=e?.reminder??(presetType==='task'?'none':'at'),type=e?.type||presetType||'appointment';const w=openSheet(`<div class="sheet-top"><div><h2>${isEdit?'Moment aanpassen':'Nieuw moment'}</h2><p class="lead">Plan iets dat je echt wilt doen.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="field"><label>Titel</label><input id="ev-title" value="${esc(e?.title||'')}" placeholder="Bijv. tandarts, sporten of lezen"></div><div class="two"><div class="field"><label>Type</label><select id="ev-type"><option value="appointment" ${type==='appointment'?'selected':''}>Afspraak</option><option value="growth" ${type==='growth'?'selected':''}>Verbeteractiviteit</option><option value="task" ${type==='task'?'selected':''}>Taak</option></select></div><div class="field"><label>Datum</label><input id="ev-date" type="date" value="${date}"></div></div><div id="growth-explain" class="growth-explain-v8 ${type==='growth'?'show':''}">${duckV8('growth-duck')}<div><b>Verbeteractiviteit</b><span>Verdien een Plus-punt nadat je de activiteit met bewijs afrondt.</span></div></div><div class="two"><div class="field"><label>Starttijd</label><input id="ev-start" type="time" value="${esc(e?.start||'')}"></div><div class="field"><label>Eindtijd</label><input id="ev-end" type="time" value="${esc(e?.end||'')}"></div></div><div class="two"><div class="field"><label>Herhalen</label><select id="ev-repeat"><option value="none">Niet</option><option value="daily" ${e?.repeat==='daily'?'selected':''}>Dagelijks</option><option value="weekly" ${e?.repeat==='weekly'?'selected':''}>Wekelijks</option><option value="monthly" ${e?.repeat==='monthly'?'selected':''}>Maandelijks</option></select></div><div class="field"><label>Melding</label><select id="ev-reminder"><option value="none" ${rem==='none'?'selected':''}>Geen</option><option value="at" ${rem==='at'?'selected':''}>Bij starttijd</option><option value="5" ${rem==='5'?'selected':''}>5 min vooraf</option><option value="10" ${rem==='10'?'selected':''}>10 min vooraf</option><option value="30" ${rem==='30'?'selected':''}>30 min vooraf</option><option value="60" ${rem==='60'?'selected':''}>1 uur vooraf</option></select></div></div><div class="sheet-actions">${isEdit?'<button id="delete-event" class="button danger">Verwijder</button>':''}<button id="save-event" class="button primary">Opslaan</button></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();const typeSelect=w.querySelector('#ev-type'),explain=w.querySelector('#growth-explain');typeSelect.onchange=()=>explain.classList.toggle('show',typeSelect.value==='growth');w.querySelector('#save-event').onclick=async()=>{const title=w.querySelector('#ev-title').value.trim(),start=w.querySelector('#ev-start').value;if(!title)return toast('Geef het moment een titel.');const obj={...(e||{}),id:e?.id||uid(),title,type:typeSelect.value,date:w.querySelector('#ev-date').value||localISO(),start,end:w.querySelector('#ev-end').value,repeat:w.querySelector('#ev-repeat').value,reminder:w.querySelector('#ev-reminder').value};if(isEdit)state.events=state.events.map(x=>x.id===obj.id?obj:x);else state.events.push(obj);save();w.remove();render();await syncReminderForEvent(obj)};w.querySelector('#delete-event')?.addEventListener('click',async()=>{state.events=state.events.filter(x=>x.id!==e.id);save();w.remove();render();try{await api7('cancel_reminder',{eventKey:`event:${e.id}`})}catch{}})}

async function disablePushV8(){try{if('serviceWorker'in navigator){const reg=await navigator.serviceWorker.ready,sub=await reg.pushManager?.getSubscription();if(sub){await api7('disable_push',{endpoint:sub.endpoint});try{await sub.unsubscribe()}catch{}}else await api7('disable_push')}else await api7('disable_push')}catch(e){try{await api7('disable_push')}catch{}throw e}server.push={...(server.push||{}),enabled:false,preference:false};await setBadge(0)}
function iosSettingsHelpV8(){const w=openSheet(`<div class="sheet-top"><div><h2>Meldingen toestaan</h2><p class="lead">iOS laat een homescreen-webapp niet betrouwbaar rechtstreeks naar haar meldinginstellingen springen.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="notice"><b>Op je iPhone:</b><br>Open Instellingen → Apps → me+ → Meldingen en zet ‘Sta meldingen toe’ aan. Open daarna me+ opnieuw.</div>`);w.querySelector('[data-close]').onclick=()=>w.remove()}
function openGeneralSettings(){const p=server.profile||{},permission=('Notification'in window?Notification.permission:'default'),pushOn=permission==='granted'&&!!server.push?.enabled,pushBlocked=permission==='denied';const w=openSheet(`<div class="sheet-top"><div><h2>Algemene instellingen</h2><p class="lead">Meldingen, privacy en jouw kennisvoorkeuren op één plek.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="section settings-first"><div class="section-head"><div><h2>Meldingen</h2><p>Krijg herinneringen voor afspraken en activiteiten.</p></div></div><button id="notify-switch" class="notify-toggle-v8 ${pushOn?'on':''} ${pushBlocked?'blocked':''}"><span class="notify-dot-v8"></span><span><b>${pushOn?'Meldingen aan':pushBlocked?'Meldingen geblokkeerd':'Meldingen uit'}</b><small>${pushOn?'Deze iPhone blijft gekoppeld.':pushBlocked?'Toestemming is op iPhone-niveau geweigerd.':'Er worden geen toekomstige reminders gepland.'}</small></span></button>${pushBlocked?'<button id="notify-help" class="button line full settings-help-v8">Bekijk iPhone-instellingen</button>':''}</div><div class="section"><div class="section-head"><div><h2>Privacy</h2><p>Bepaal wat anderen van jouw voortgang mogen zien.</p></div></div>${settingToggle('shareTasks','Deel activiteiten','Vrienden kunnen zien welke verbeteractiviteiten je voltooit.',p.shareTasks)}${settingToggle('shareProof','Deel bewijsfoto’s','Alleen bij gedeelde activiteiten en alleen met vrienden.',p.shareProof)}${settingToggle('shareProgress','Deel voortgang','Laat punten, level en rang aan vrienden zien.',p.shareProgress)}<div class="setting"><div class="setting-main"><div class="setting-title">Wie ziet je profiel?</div><div class="setting-sub">Kies of niet-vrienden je voortgang al mogen zien.</div></div><select id="visibility" style="width:auto"><option value="everyone" ${p.profileVisibility==='everyone'?'selected':''}>Iedereen</option><option value="friends" ${p.profileVisibility==='friends'?'selected':''}>Vrienden</option></select></div><div class="setting"><div class="setting-main"><div class="setting-title">Vriendverzoeken</div><div class="setting-sub">Bepaal of anderen je mogen toevoegen.</div></div><select id="requests" style="width:auto"><option value="everyone" ${p.friendRequestPolicy==='everyone'?'selected':''}>Iedereen</option><option value="nobody" ${p.friendRequestPolicy==='nobody'?'selected':''}>Niemand</option></select></div></div><div class="section"><div class="section-head"><div><h2>Interesses</h2><p>Bepaal welke kennis je vaker ziet.</p></div></div><div class="genre-grid" id="settings-interests">${CATEGORIES.map(c=>`<button class="genre ${(p.interests||[]).includes(c)?'selected':''}" data-settings-genre="${esc(c)}">${svg(categoryIcon(c))}<b>${esc(c)}</b></button>`).join('')}</div></div><div class="section"><button id="settings-save" class="button primary full">Instellingen opslaan</button></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();w.querySelectorAll('[data-settings-genre]').forEach(b=>b.onclick=()=>b.classList.toggle('selected'));w.querySelectorAll('[data-setting]').forEach(b=>b.onclick=()=>b.classList.toggle('on'));w.querySelector('#notify-help')?.addEventListener('click',iosSettingsHelpV8);w.querySelector('#notify-switch').onclick=async()=>{const b=w.querySelector('#notify-switch');if(pushBlocked){iosSettingsHelpV8();return}b.disabled=true;try{if(b.classList.contains('on')){await disablePushV8();toast('Meldingen uitgeschakeld');w.remove();openGeneralSettings()}else{await enablePush();server.push={...(server.push||{}),enabled:true,preference:true};await syncAllReminders();toast('Meldingen ingeschakeld');w.remove();openGeneralSettings()}}catch(e){toast(e.message)}finally{if(document.body.contains(b))b.disabled=false}};w.querySelector('#settings-save').onclick=async()=>{const interests=[...w.querySelectorAll('[data-settings-genre].selected')].map(x=>x.dataset.settingsGenre);try{const r=await api('settings',{interests,shareTasks:w.querySelector('[data-setting="shareTasks"]').classList.contains('on'),shareProof:w.querySelector('[data-setting="shareProof"]').classList.contains('on'),shareProgress:w.querySelector('[data-setting="shareProgress"]').classList.contains('on'),profileVisibility:w.querySelector('#visibility').value,friendRequestPolicy:w.querySelector('#requests').value});server.profile=r.profile;state.knowledgeSeen=true;save();await refreshBase();w.remove();render();toast('Instellingen opgeslagen.')}catch(e){toast(e.message)}}}

function achievementsV8(){const p=server.profile||{},s=server.stats||{},st=Number(server.extras?.streak||0);return [{ok:Number(s.activities||0)>=1,icon:'trophy',title:'Eerste activiteit',sub:'Je eerste verbeteractiviteit voltooid'},{ok:st>=7,icon:'flame',title:'7 dagen reeks',sub:'Een volledige week echte voortgang'},{ok:Number(p.points||0)>=100,icon:'spark',title:'100 punten',sub:'Honderd echte punten verdiend'},{ok:Number(s.reads||0)>=10,icon:'knowledge',title:'10 kennisitems',sub:'Tien artikelen echt uitgelezen'},{ok:Number(s.activities||0)>=25,icon:'trend',title:'25 verbeteractiviteiten',sub:'Vijfentwintig keer bewijs geleverd'}]}
function profileView(){const p=server.profile||{},stats=server.stats||{},nr=nextRank(p.points||0),ach=achievementsV8();return shell(`<div class="profile-head"><div class="avatar lg">${esc(initials(p.name||state.profileName))}</div><div><h1>${esc(p.name||state.profileName)}</h1><div class="handle">@${esc(p.handle||'')} · Level ${p.level||1} · ${esc(p.rank||'Starter')}</div></div></div><div class="section" style="margin-top:0"><div class="section-head"><div><h2>${p.points||0} punten</h2><p>${nr?`${nr[1]-(p.points||0)} punten tot ${nr[0]}`:'Hoogste rang bereikt'}</p></div></div><div class="progress"><span style="width:${progress(p)}%"></span></div></div><section class="section"><div class="stats"><div class="stat"><strong>${stats.friendCount||server.friends.length||0}</strong><span>vrienden</span></div><div class="stat"><strong>${stats.activities||0}</strong><span>activiteiten</span></div><div class="stat"><strong>${stats.reads||0}</strong><span>artikelen</span></div></div></section><section class="section"><div class="section-head"><div><h2>Mijlpalen</h2><p>Kleine bewijzen dat je blijft doorgaan.</p></div></div><div class="achievement-grid-v8">${ach.map(x=>`<div class="achievement-v8 ${x.ok?'earned':'locked'}"><span>${x.icon==='knowledge'?svg('knowledge'):svg(x.icon)}</span><div><b>${esc(x.title)}</b><small>${esc(x.sub)}</small></div>${x.ok?'<i>✓</i>':''}</div>`).join('')}</div></section><section class="section"><div class="section-head"><div><h2>Profiel</h2><p>Beheer wat me+ voor jou doet.</p></div></div><div class="menu"><button class="menu-btn" id="general-settings"><span class="menu-icon">${svg('settings')}</span><span class="menu-main"><span class="menu-title">Algemene instellingen</span><span class="menu-sub">Meldingen, privacy, zichtbaarheid en interesses</span></span><span class="chev">›</span></button><button class="menu-btn" id="rank-info"><span class="menu-icon">${svg('ranks')}</span><span class="menu-main"><span class="menu-title">Rangen & punten</span><span class="menu-sub">Bekijk alle levels en hoeveel punten je nodig hebt</span></span><span class="chev">›</span></button><button class="menu-btn" data-nav="knowledge" data-filter-jump="Bewaard"><span class="menu-icon">${svg('bookmark')}</span><span class="menu-main"><span class="menu-title">Bewaarde kennis</span><span class="menu-sub">${server.extras.saved||0} opgeslagen artikelen</span></span><span class="chev">›</span></button><button class="menu-btn"><span class="menu-icon">${svg('flame')}</span><span class="menu-main"><span class="menu-title">Jouw dagreeks</span><span class="menu-sub">${server.extras.streak||0} dagen met echte voortgang</span></span></button></div></section>`)}

function premiumPopV8(text){const old=document.querySelector('.reward-v8');old?.remove();const p=document.createElement('div');p.className='reward-v8';p.innerHTML=`<div class="reward-confetti-v8">${Array.from({length:8},(_,i)=>`<i style="--i:${i}"></i>`).join('')}</div>${duckV8('reward-duck')}<strong>${esc(text)}</strong><span>Mooi. Dit telt.</span>`;document.body.appendChild(p);hapticV8();requestAnimationFrame(()=>p.classList.add('show'));setTimeout(()=>{p.classList.add('leave');setTimeout(()=>p.remove(),350)},1450)}
function pointPop(t){premiumPopV8(t);setTimeout(cancelCompletedRemindersV8,0)}
async function cancelCompletedRemindersV8(){for(const e of state.events||[]){if(state.done?.[doneKey(e,localISO())])await syncReminderForEvent(e)}}
function miniDoneV8(){const p=document.createElement('div');p.className='mini-done-v8';p.innerHTML=`${duckV8('mini-done-duck')}<span>Afgerond</span>`;document.body.appendChild(p);hapticV8();setTimeout(()=>p.remove(),850)}
function levelUpV8(level,rank){const p=document.createElement('div');p.className='levelup-v8';p.innerHTML=`<div class="levelup-card-v8"><small>LEVEL UP</small>${duckV8('level-duck')}<strong>Level ${level}</strong><span>${esc(rank||'Nieuwe rang')} · weer een stap verder.</span></div>`;document.body.appendChild(p);hapticV8();setTimeout(()=>p.classList.add('show'),20);setTimeout(()=>{p.classList.add('leave');setTimeout(()=>p.remove(),350)},1900)}
function maybeStreakMilestoneV8(){const s=Number(server.extras?.streak||0);if(!V8_STREAK_MILESTONES.includes(s)||state.v8StreakSeen===s)return;state.v8StreakSeen=s;save();const p=document.createElement('div');p.className='streak-pop-v8';p.innerHTML=`<b>🔥 ${s} dagen</b><span>Je reeks groeit.</span>`;document.body.appendChild(p);setTimeout(()=>p.remove(),1500)}
const refreshBaseV8Base=refreshBase;
refreshBase=async function(){await refreshBaseV8Base();const lvl=Number(server.profile?.level||1);if(state.v8KnownLevel==null){state.v8KnownLevel=lvl;save()}else if(lvl>Number(state.v8KnownLevel)){state.v8KnownLevel=lvl;save();setTimeout(()=>levelUpV8(lvl,server.profile?.rank),200)}else if(lvl!==Number(state.v8KnownLevel)){state.v8KnownLevel=lvl;save()}}
const bindV8Base=bind;
bind=function(){bindV8Base();el('quick-add')?.addEventListener('click',quickActionsV8);document.querySelectorAll('[data-check]').forEach(b=>b.onclick=async()=>{const e=state.events.find(x=>x.id===b.dataset.check);if(!e)return;const k=doneKey(e,b.dataset.date),was=!!state.done[k];state.done[k]=!was;save();render();await syncReminderForEvent(e);if(!was)miniDoneV8()})}
async function registerSW(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register('/sw.js?v=8');reg.update().catch(()=>{})}catch(e){console.warn('sw',e)}}