function openProof(e,date){const w=openSheet(`<div class="sheet-top"><div><h2>Bewijs toevoegen</h2><p class="lead">Maak een nieuwe foto of kies één foto uit je galerij.</p></div><button class="icon-btn" data-close>${svg('close')}</button></div><div class="v9-proof-choices"><button id="v9-camera">${svg('camera')}<span><b>Foto maken</b><small>Open de camera</small></span></button><button id="v9-gallery"><svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="16" rx="2"/><circle cx="8" cy="9" r="1.5"/><path d="m5 17 4-4 3 3 2-2 5 5"/></svg><span><b>Kies uit galerij</b><small>Deel alleen de foto die jij kiest</small></span></button></div><input id="v9-camera-input" class="v9-hidden" type="file" accept="image/jpeg,image/png,image/webp" capture="environment"><input id="v9-gallery-input" class="v9-hidden" type="file" accept="image/jpeg,image/png,image/webp"><div id="v9-picked" class="v9-picked" hidden><img id="v9-preview" class="proof-preview" alt="Bewijs preview"><div><b>Foto gekozen</b><button class="link" id="v9-change">Andere foto kiezen</button></div></div><div class="notice">Zonder geldig bewijs krijg je geen punt. Dezelfde foto kan niet twee keer worden gebruikt.</div><div class="sheet-actions"><button id="v9-proof-submit" class="button primary" disabled>Bevestig bewijs · +1 punt</button></div>`);w.querySelector('[data-close]').onclick=()=>w.remove();const cam=w.querySelector('#v9-camera-input'),gal=w.querySelector('#v9-gallery-input'),preview=w.querySelector('#v9-preview'),picked=w.querySelector('#v9-picked'),submit=w.querySelector('#v9-proof-submit');let dataUrl='',busy=false;w.querySelector('#v9-camera').onclick=()=>cam.click();w.querySelector('#v9-gallery').onclick=()=>gal.click();w.querySelector('#v9-change').onclick=()=>gal.click();const choose=async ev=>{const f=ev.target.files?.[0];if(!f)return;if(f.size>6*1024*1024)return toast('Kies een foto kleiner dan 6 MB.');if(!/^image\/(jpeg|png|webp)$/.test(f.type))return toast('Gebruik een JPG, PNG of WebP-foto.');dataUrl=await fileData(f);preview.src=dataUrl;picked.hidden=false;submit.disabled=false};cam.onchange=choose;gal.onchange=choose;submit.onclick=async()=>{if(!dataUrl||busy)return;busy=true;submit.disabled=true;submit.textContent='Controleren…';try{const r=await api('complete_activity',{eventKey:`task:${doneKey(e,date)}`,title:e.title,localDate:date,dataUrl,metadata:{eventId:e.id,date}});state.done[doneKey(e,date)]=true;save();if(server.profile){server.profile.points=r.points;server.profile.level=r.level;server.profile.rank=r.rank;server.profile.next=r.next}await syncReminderForEvent(e);await refreshBase();w.remove();render();premiumPopV8('+1 punt')}catch(err){toast(err.message);busy=false;submit.disabled=false;submit.textContent='Bevestig bewijs · +1 punt'}}}

function v9StopReader(){clearInterval(readTimer);clearInterval(readSyncTimer);clearInterval(v9Paint);clearInterval(v9Sync);readTimer=readSyncTimer=v9Paint=v9Sync=null;if(v9Vis){document.removeEventListener('visibilitychange',v9Vis);v9Vis=null}if(v9Key){document.removeEventListener('keydown',v9Key);v9Key=null}}
function v9ShownActive(){if(!readSession)return 0;const base=Number(readSession.v9Base||0);return document.hidden||readSession.v9At==null?base:base+(performance.now()-readSession.v9At)/1000}
function paintReadTimer(){const t=el('read-timer');if(!t||!readSession)return;const remain=Math.max(0,Math.ceil(Number(readSession.min||0)-v9ShownActive()));t.innerHTML=`<span><strong>${fmtSeconds(remain)}</strong>${remain?'actief lezen':'controleren…'}</span>`;if(!remain&&!readSession.syncing)syncReadProgressV9()}
async function syncReadProgressV9(){if(!readSession?.sessionId||readSession.syncing||readSession.complete||document.hidden)return;readSession.syncing=true;try{const r=await api7('read_tick_v7',{sessionId:readSession.sessionId});readSession.active=Number(r.activeSeconds||0);readSession.min=Number(r.minSeconds||readSession.min||0);readSession.v9Base=readSession.active;readSession.v9At=performance.now();paintReadTimer();if(r.done)await finishRead()}catch(e){console.warn(e)}finally{if(readSession)readSession.syncing=false}}
async function openArticle(a){if(!a)return;closeArticle(false);let start={alreadyRead:true,minSeconds:0,sessionId:null,activeSeconds:0};try{start=await api7('start_read_v7',{articleId:a.id})}catch(e){console.warn(e)}readSession={article:a,sessionId:start.sessionId,min:Number(start.minSeconds||0),active:Number(start.activeSeconds||0),v9Base:Number(start.activeSeconds||0),v9At:performance.now(),alreadyRead:start.alreadyRead,complete:false,syncing:false};const page=document.createElement('div');page.className='article-page v9-article';page.id='article-page';page.innerHTML=`<div class="article-inner"><button type="button" class="icon-btn article-close" id="article-close" aria-label="Sluiten">${svg('close')}</button><div class="v9-article-visual ${v9Tone(a.category)}">${svg(categoryIcon(a.category))}<span>${esc(a.category)}</span></div><div class="article-cat">${esc(a.category)} · ${readLabel(a)}</div><h1>${esc(a.title)}</h1><div class="article-lead">${esc(a.intro)}</div><div class="article-save"><button class="button line small" id="article-save">${server.saved.has(a.id)?'✓ Bewaard':'Bewaar voor later'}</button></div><div class="article-body"><p>${esc(a.body)}</p>${a.extra_fact?`<h3>Extra feit</h3><p>${esc(a.extra_fact)}</p>`:''}${a.why_it_matters?`<h3>Waarom dit interessant is</h3><p>${esc(a.why_it_matters)}</p>`:''}</div></div>${start.alreadyRead?'<div class="timer done"><span><strong>✓</strong>gelezen</span></div>':`<div class="timer" id="read-timer"><span><strong>${fmtSeconds(Math.max(0,readSession.min-readSession.active))}</strong>actief lezen</span></div>`}`;document.body.appendChild(page);document.body.classList.add('v9-reading');page.querySelector('#article-close').onclick=ev=>{ev.preventDefault();ev.stopPropagation();closeArticle()};page.querySelector('#article-save').onclick=async ev=>{ev.preventDefault();ev.stopPropagation();await toggleSave(a.id);const b=page.querySelector('#article-save');if(b)b.textContent=server.saved.has(a.id)?'✓ Bewaard':'Bewaar voor later'};v9Key=ev=>{if(ev.key==='Escape')closeArticle()};document.addEventListener('keydown',v9Key);if(!start.alreadyRead&&start.sessionId){v9Paint=setInterval(paintReadTimer,250);v9Sync=setInterval(syncReadProgressV9,4000);v9Vis=()=>{if(!readSession)return;if(document.hidden){readSession.v9Base=v9ShownActive();readSession.v9At=null}else{readSession.v9At=performance.now();syncReadProgressV9()}paintReadTimer()};document.addEventListener('visibilitychange',v9Vis);paintReadTimer()}}
async function readTick(){return syncReadProgressV9()}
async function finishRead(){if(readSession?.complete||!readSession?.sessionId)return;readSession.complete=true;v9StopReader();try{const r=await api7('complete_read_v7',{sessionId:readSession.sessionId,localDate:localISO()});const t=el('read-timer');if(t){t.classList.add('done');t.innerHTML='<span><strong>✓</strong>gelezen</span>'}if(r.firstRead){premiumPopV8('+1 kennispunt');await refreshBase()}}catch(e){readSession.complete=false;if(e?.data?.remaining!=null)readSession.v9Base=Math.max(0,readSession.min-Number(e.data.remaining||0));readSession.v9At=performance.now();v9Paint=setInterval(paintReadTimer,250);v9Sync=setInterval(syncReadProgressV9,4000);paintReadTimer()}}
function closeArticle(redraw=true){v9StopReader();readSession=null;document.querySelectorAll('#article-page').forEach(x=>x.remove());document.body.classList.remove('v9-reading');if(redraw)render()}
async function registerSW(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register('./sw.js?v=9p');reg.update().catch(()=>{})}catch(e){console.warn('sw',e)}}
