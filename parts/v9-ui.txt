/* me+ v9: reader reliability, proof picker, themes and visual identity */
const V9_THEME='meplus-theme-v9';
let v9Paint=null,v9Sync=null,v9Vis=null,v9Key=null;
function v9ThemeChoice(){return localStorage.getItem(V9_THEME)||'system'}
function v9ApplyTheme(choice=v9ThemeChoice()){const dark=choice==='dark'||(choice==='system'&&matchMedia('(prefers-color-scheme:dark)').matches);document.documentElement.dataset.theme=dark?'dark':'light';document.documentElement.dataset.themeChoice=choice;document.documentElement.style.colorScheme=dark?'dark':'light';const m=document.querySelector('meta[name="theme-color"]');if(m)m.content=dark?'#101816':'#f4f1e9'}
function v9SetTheme(choice){localStorage.setItem(V9_THEME,choice);v9ApplyTheme(choice)}
if(!window.__v9Theme){window.__v9Theme=true;matchMedia('(prefers-color-scheme:dark)').addEventListener?.('change',()=>{if(v9ThemeChoice()==='system')v9ApplyTheme('system')})}
const v9BootBase=boot;boot=async function(){v9ApplyTheme();return v9BootBase()};

function v9Week(){const now=new Date(),dow=(now.getDay()+6)%7,start=new Date(now);start.setDate(now.getDate()-dow);start.setHours(12,0,0,0);let planned=0,done=0;for(let i=0;i<=dow;i++){const d=new Date(start);d.setDate(start.getDate()+i);const key=localISO(d),items=eventsFor(key);planned+=items.length;done+=items.filter(e=>state.done?.[doneKey(e,key)]).length}return{planned,done,pct:planned?Math.round(done/planned*100):0}}
function v9Ring(p,done,total){p=Math.max(0,Math.min(100,Math.round(p||0)));return `<div class="v9-ring" style="--v9deg:${p*3.6}deg"><div><b>${p}%</b><span>${done}/${total}</span></div></div>`}
function v9Recap(){const w=v9Week();return `<div class="v9-recap"><div><span>Jouw week in Me+</span><b>${w.done} momenten afgerond</b></div>${duckV8('v9-recap-duck')}<div class="v9-recap-stats"><i><b>${w.pct}%</b><span>planning</span></i><i><b>${server.extras?.streak||0}</b><span>dagen reeks</span></i><i><b>${server.stats?.reads||0}</b><span>kennis totaal</span></i></div></div>`}
function todayView(){const p=server.profile,a=server.articles[0],date=localISO(),items=eventsFor(date),done=items.filter(e=>state.done[doneKey(e,date)]).length,total=items.length,pct=total?done/total*100:0,next=nextRank(p?.points||0),week=v9Week();return shell(`<section class="v9-day-card"><div><div class="eyebrow">${esc(fmtDate())}</div><h1 class="greeting-v8"><span>${greeting()},</span><span class="full-name-v8">${esc(p?.name||state.profileName)}</span></h1><p>${total?`${done} van ${total} activiteiten voltooid.`:'Je dag is nog open. Eén goede stap is genoeg.'}</p><div class="v9-daybar"><i style="width:${pct}%"></i></div></div>${v9Ring(pct,done,total)}</section>${a?`<article class="hero v9-knowledge-hero" data-article="${a.id}"><div class="v9-hero-icon">${svg(categoryIcon(a.category))}</div><div class="meta">Weetje van vandaag · ${esc(a.category)}</div><h2>${esc(a.title)}</h2><p>${esc(a.intro)}</p><div class="hero-foot"><button class="hero-btn" data-article="${a.id}">Lees verder</button><span class="read-time">${readLabel(a)} · +1 bij echt lezen</span></div></article>`:''}<section class="meplus-moment-v8 v9-moment"><div><span>Jouw Me+ moment</span><p>${esc(momentV8())}</p></div>${duckV8('moment-duck')}</section><section class="section"><div class="section-head"><div><h2>Vandaag</h2><p>Afspraken en verbeteractiviteiten</p></div><button id="quick-add" class="quick-add-v8" aria-label="Snel toevoegen">+</button></div>${items.length?`<div class="list v9-event-list">${items.map(e=>eventRow(e,date)).join('')}</div>`:'<div class="empty v9-empty">Nog niets gepland. Voeg iets toe dat vandaag telt.</div>'}</section><section class="section"><div class="section-head"><div><h2>Jouw voortgang</h2><p>${esc(feedbackV8())}</p></div></div><div class="v9-metrics"><div class="growth"><span>${svg('flame')}</span><b>${server.extras?.streak||0}</b><small>dagen reeks</small></div><div class="gold"><span>${svg('trophy')}</span><b>${p?.points||0}</b><small>punten</small></div><div class="personal"><span>${svg('trend')}</span><b>Lv. ${p?.level||1}</b><small>${esc(p?.rank||'Starter')}</small></div><div class="knowledge-tone"><span>${svg('knowledge')}</span><b>${week.pct}%</b><small>deze week</small></div></div></section><section class="section">${v9Recap()}</section>`) }

function v9Tone(c){if(['Ruimte','Wetenschap','Technologie'].includes(c))return'knowledge-tone';if(['Psychologie','Cultuur'].includes(c))return'personal';if(['Natuur','Natuurkennis','Menselijk lichaam'].includes(c))return'growth';return'gold'}
function knowledgeCard(a){return `<article class="knowledge v9-knowledge ${v9Tone(a.category)}"><div class="v9-knowledge-icon">${svg(categoryIcon(a.category))}</div><div data-article="${a.id}"><div class="cat">${esc(a.category)}</div><h3>${esc(a.title)}</h3><p>${esc(a.intro)}</p><div class="knowledge-meta"><span>${readLabel(a)}</span><span>${words(a)>300?'lang verhaal':words(a)>180?'verdieping':'kort weetje'}</span></div></div><button class="save-btn ${server.saved.has(a.id)?'saved':''}" data-save="${a.id}" aria-label="Bewaren">${svg('bookmark')}</button></article>`}

const v9SettingsBase=openGeneralSettings;openGeneralSettings=function(){v9SettingsBase();const wraps=document.querySelectorAll('.sheet-wrap'),w=wraps[wraps.length-1];if(!w||w.querySelector('.v9-theme-section'))return;const first=w.querySelector('.settings-first');if(!first)return;const sec=document.createElement('div');sec.className='section v9-theme-section';const choice=v9ThemeChoice();sec.innerHTML=`<div class="section-head"><div><h2>Thema</h2><p>Kies hoe me+ eruitziet.</p></div></div><div class="v9-theme-picker">${[['light','Licht'],['dark','Donker'],['system','Systeem']].map(([k,l])=>`<button data-v9-theme="${k}" class="${choice===k?'active':''}">${l}</button>`).join('')}</div>`;first.insertAdjacentElement('afterend',sec);sec.querySelectorAll('[data-v9-theme]').forEach(b=>b.onclick=()=>{v9SetTheme(b.dataset.v9Theme);sec.querySelectorAll('[data-v9-theme]').forEach(x=>x.classList.toggle('active',x===b))})};
